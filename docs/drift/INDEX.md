# Drift Report Index

Source of truth for build progress. On any restart, read this file, find the last phase that passed its gate, and continue from the next one. Do not restart from Phase A.

Protocol: per phase, build, verify (typecheck, build, acceptance checks, regression/parity contract, em-dash sweep, test suite from Phase B on), write the drift report, remediate until truly green, then advance. PAUSE_AT_MILESTONES = true.

Milestones (hard stops for owner review): C, G, H and I, K, T, X, AI, AJ.

| Phase | Name | Verdict | Milestone |
| --- | --- | --- | --- |
| A | Grounding | Pass | no |
| B | Foundations | Pass | no |
| C | Cortex and Confounder | Pass | yes |
| D | Auth, Orgs and Access | Pass | no |
| E | Product Surfaces | Pass | no |
| F | Fast Seeding and World-Class Seed Data | Pass | no |
| G | Parity Gate and Core Build Report | Pass | yes |
| H | Connector Framework and Registry | Pass | yes |
| I | Connected Mode, Edge Agent, and Runtime No-Write Guard | Pass | yes |
| J | Split Pipeline (Tier 2, the Lens In-Boundary) | Pass | no |
| K | Tier 3: Cryptographic Isolation, No Standing Access, Hash-Chained Provenance | Pass | yes (paused for owner review) |
| L | Connected Portal Security Surfaces (Posture, Connections, Break-glass, Provenance) | Pass | no (gated, paused for owner review) |
| M | Stage 2 Full Verification and the Build-Report Append | Pass | no (gated, paused for owner review) |
| N | Cost and Token Observability | Pass | no (gated, paused for owner review) |
| O | Connector Operational Reality | Pass | no (gated, autonomous O-P-Q run) |
| P | Observability and Alerting | Pass | no (gated, autonomous O-P-Q run) |
| Q | Secrets Vault | Pass | no (gated, autonomous O-P-Q run; owner review after Q) |
| R | Expand Test Coverage and Confirm CI | Pass | no (gated, autonomous R-S-T run) |
| S | Retention and Deletion | Pass | no (gated, autonomous R-S-T run) |
| T | Client Onboarding Experience | Pass | yes (milestone hard-stop, paused for owner review) |
| U | Backups and Disaster Recovery | Pass | no (gated, Stage 3; autonomous U-V-W run) |
| V | Verification and the Build-Report Append (closes Stage 3) | Pass | no (gated, Stage 3; autonomous U-V-W run) |
| W | Outcome Loop and Value Realized (opens Stage 4) | Pass | no (gated, Stage 4; autonomous U-V-W run; hard stop after W before milestone X) |
| X | Benchmarking and the Data Network Effect | Pass | yes (security milestone, hard stop for owner review) |
| Y | Portfolio Intelligence View | Pass | no (gated, Stage 4; autonomous Y-Z-AA-AB-AC run, pauses at Stage 4/5 boundary before AD) |
| Z | Proactive Push Intelligence | Pass | no (gated, Stage 4; autonomous Y-Z-AA-AB-AC run, pauses at Stage 4/5 boundary before AD) |
| AA | Interactive Challenge | Pass | no (gated, Stage 4; autonomous Y-Z-AA-AB-AC run, pauses at Stage 4/5 boundary before AD) |
| AB | Sellability Pack | Pass | no (gated, Stage 4; autonomous Y-Z-AA-AB-AC run, pauses at Stage 4/5 boundary before AD) |
| AC | Verification and Build Report (closes Stage 4) | Pass | no (gated, Stage 4; autonomous Y-Z-AA-AB-AC run, pauses at Stage 4/5 boundary before AD) |
| AD | Experience Audit and Drift Fix (opens Stage 5) | Pass | no (gated, Stage 5; single owner-authorized phase, pauses at its own gate before AE) |
| AE | Ingestion Suite | Pass | no (gated, Stage 5; autonomous AE-AF-AG-AH-AI run, pauses at the AI milestone) |
| AF | Local LLM Seat and Sovereign Mode | Pass | no (gated, Stage 5; PAUSED at the AF gate on the real-endpoint blocker, does not auto-advance to AG) |
| AG | Curated Custom-Layer Creation Flow | Pass | no (gated, Stage 5; autonomous AE-AF-AG-AH-AI run, pauses at the AI milestone) |
| AH | Cloud Portability | Pass | no (gated, Stage 5; autonomous AE-AF-AG-AH-AI run, pauses at the AI milestone) |
| AI | Verification and the Build-Report Append (closes Stage 5) | Pass | yes (milestone hard stop; closes Stage 5, end of the autonomous AE-AF-AG-AH-AI run, paused for owner review) |
| AJ | The Brier-Scored Calibration Ledger | Pass | yes (milestone hard stop; supersedes Phase W loose calibration, paused for owner review) |
| AK | The Data Efficacy Index | Pass | no (gated, opens Stage 6; owner-authorized AK-AL-AM-AN sequence run linearly, advances to AL) |
| AL | The Decision Ledger and Pre-mortem | Pass | no (gated, Stage 6; owner-authorized AK-AL-AM-AN sequence run linearly, advances to AM) |
| AM | As-of Replay and the Diligence Pack | Pass | no (gated, Stage 6; owner-authorized AK-AL-AM-AN sequence run linearly, advances to AN) |
| AN | Final Verification and the Consolidated Report (closes Stage 6 and the whole build) | Pass | yes (closing milestone of Stage 6 and the whole build; owner-authorized AK-AL-AM-AN sequence complete, build closed) |
| AO | Priority Connectors (opens the Robustness and Magic wave) | Pass | no (gated; reopens the build closed at AN as the first of the AO-AS Robustness and Magic wave; advances to AP) |
| AP | Sovereign Seat Realisation (correctness audit) | Pass | no (gated, Robustness and Magic wave; advances to AQ) |
| AQ | Outcome Loop Closure | Pass | no (gated, Robustness and Magic wave; advances to AR) |
| AR | Operational Hardening | Pass | no (gated, Robustness and Magic wave; advances to AS) |
| AS | Signature Surface (closes the Robustness and Magic wave) | Pass | no (gated, Robustness and Magic wave; closes the AO-AS wave) |

## Notes

- V1 reference: cloned read-only into `reference/v1` (gitignored). Re-clone each session from the repo recorded in memory.
- Phase C gate met: one real tenant (Patagonia) seeded end to end, fourteen of fourteen layers built, live three-model cortex and grounded Confounder, per-seat telemetry readable via routes. See `phase-C.md`. Milestone pause before Phase D.
- Model API keys (Anthropic, Gemini) are wired via the AI integrations env vars and were exercised live by the Phase C seed.
- Phase D gate met: PIN-gated registration, owner-minted and scoped PINs, client and portfolio tenant fencing, owner Access console, scrypt passwords and HMAC PIN hashing with zero new dependencies. Owner bootstraps from secrets (one provider org, one active owner confirmed in the database). The four PIN failure modes return one byte-identical error. See `phase-D.md`. Not a milestone; continuing to Phase E.
- Phase E gate met: the full per-tenant portal built to the V1 design language at or above parity from real persisted data only. Surfaces: Morning Brief, Board Pack, Layer pages with 8 archetype heroes, Intelligence Architecture, Data Heartbeat, Anomaly Inbox, Dependency Map, Ask Different Day, War Room, Track Record, Connections, perspective lens, boot splash. Diagnosis in two clicks; designed loading/ready/empty/error/no-tenant states; zero new npm deps; no em-dash or en-dash; AA via the audited token system; 375px. Pure derivations are unit-tested; api-server 73 and portal 105 tests pass; build green at 1728 modules. Logged drift: `GET /api/tenants` is a deliberate, access-fenced reversal of Phase D's no-list stance; real `committed_actions` with honest pending states and no fabricated outcomes; live cortex ask and interactive war-room simulation deferred to avoid fabrication; built against the one seeded tenant with cross-tenant breadth deferred to Phase F. See `phase-E.md`. Not a milestone; continuing to Phase F.
- Phase F gate met: the seed engine is rebuilt on a Postgres-backed claim queue (the queue brought forward from Phase AH as a new, separate `pipeline_jobs` table) replacing the in-module limiter, with Anthropic prompt-cache reuse, intra-layer parallelism, a single batched Evaluator call (recorded once, summed without triple-counting), and an honest express mode (confound and challenge skipped on non-priority layers, marked reduced end to end). Four real companies are seeded to ready with zero pipeline errors and verifiably distinct figures: Patagonia, The Hillman Group, Lattice, Hinge Health. Live timings at LAYER_CONCURRENCY=2: express 41.6 min (Hillman), full 46.8 min (Lattice) and 50.3 min (Hinge), express-to-full upgrade 34.4 min rebuilding only the nine reduced layers; express is about 11 to 17 percent faster end to end than full. The cross-tenant anchor-figure sweep passes; zero new npm deps; no em-dash or en-dash; cortex 42, api-server 85 and portal 108 tests pass. Logged drift: the queue is brought forward from AH; the anchor sweep was recalibrated from "any shared currency figure fails" to a templating-signature test (pair specifics or overlap, plus a broadcast rule failing a specific figure stated by three or more tenants; a grep proves no collision leaks from prompts); Patagonia (about $1.47B) and Hillman (about $1.5B) are the same scale so they genuinely share a $1.47 billion reported-revenue figure; live concurrency was held at 2 to avoid Anthropic 429s; score-stage basis fragility (self-corrected by retry) is now remediated (coerced to modelled at the score boundary, stored content strict). The two architect-flagged hardenings (the anchor broadcast rule and the score-basis tolerance) landed with the anchor logic extracted to a unit-tested pure module; see the Remediation iterations in `phase-F.md`. Not a milestone, but Phase G next is a milestone hard-stop for owner review.
- Phase G gate met (milestone): every named V1 reference surface (boot splash, narrator voice, verified-versus-modelled pills, Morning Brief, Board Pack, Ask Different Day, scenario war room, anomaly inbox, dependency map) is present in the V2 portal and meets or beats V1; the Phase B through F acceptance sets re-verify; typecheck, build, and the full suite (scripts 4, cortex 48, db 8, portal 108, api-server 85 = 253) are green; zero new npm deps. The long-dash sweep is now zero across code, copy, AND data: the gate found that `tenant_pipeline_runs.sub_stages` persists raw model output that the source-only guard cannot see (39 contaminated run rows), so the sanitizer is now wired at that persist boundary too, the cleanup script and the database-wide sweep cover the run table and `pipeline_jobs`, and the rows were cleaned to zero; the source guard was strengthened to catch the en-dash as well as the em-dash. Parity was verified at the code and component level against the frozen `reference/v1` plus the automated suite and the Phase E side-by-side, not a live two-instance dual-deploy (logged as accepted drift). Three V1 extras (company picker and library, coachmark tour, signal ticker) are deliberately not carried. The core build report is `docs/build-report-core.md`; the drift report is `phase-G.md`. This is a milestone hard-stop: PAUSED for owner review before Phase H, do not auto-advance.
- Owner secrets (OWNER_EMAIL, OWNER_PASSWORD, SESSION_SECRET) are injected into the workflow processes only, not the agent shell or sandbox, so live owner login is verified via the integration suite and the bootstrapped owner row rather than an interactive curl.
- Git tagging: this is a Replit managed-VCS environment where commits are created as automatic checkpoints. This INDEX is the protocol's stated source of truth for progress in place of manual `phase-<id>` tags. Logged as acceptable drift.
- Post-X audit and remediation (2026-06-15): an owner-requested drift audit of Phases A through X actioned three in-scope fixes in the benchmark cohort read path (a read-time k re-gate against the current floor, a typed self-marker catch in place of a bare catch, and a route-level flip of a stuck "seeding" tenant to "failed"), all covered by the green suite. The remaining items are accepted or deferred with operator actions. The two-sided long-dash sweep is zero (the source guard in the suite plus a database-wide sweep over 118 text and jsonb columns across 32 tables); typecheck, build, and test exit 0. No phase was minted and no gate advanced; Phase X remains the milestone hard stop. See `docs/drift/audit-post-X.md`.
- Phase H gate met (milestone): the V2 data-connector addendum starts here. A new internal workspace package `lib/connectors` defines one uniform connector contract with a capability-minimal context (resolveSecret, tokenize, now, log; no database handle, no filesystem), a full catalogue of 46 connectors across all ten Part 1 families each mapped to canonical layer keys, a registry that returns an honest "available, not connected" error for declared-but-unimplemented connectors, and two bring-your-own-warehouse reference connectors (`generic-sql`, `redshift`) proven end to end against a real Postgres-wire warehouse through the derive-and-discard path (measures declared as a structured parameterized DSL with no free-form SQL, the aggregate SELECT built by the connector so a raw column can never be projected, read-only transaction, numeric-only column, every return guarded by `assertDerivedSignalSet`, connection discarded). The Part 4 schema (connectors, tenant_connections, connector_runs, derived_signals, provenance_ledger, tenant_keys, plus a tenants.dataMode column) is pushed. The catalogue is seeded into the connectors table (46 rows) idempotently. Derive-and-discard is enforced and tested: the connector path imports only `@workspace/db/contracts` (never the db root that opens the app pool), proven by a static import-boundary test; a connector returning raw content fails loudly. Typecheck, build, and the full suite are green (21 new connectors tests); zero new npm deps (pg and @types/pg were already in the lockfile); the long-dash sweep is zero across source and the seeded catalogue. Routes, the connected-mode pipeline branch, the split pipeline, provenance writes, and the portal screens are deliberately Phase I and later. The drift report is `phase-H.md`; the V2 build report is `docs/build-report-v2.md`. This is a milestone hard-stop: PAUSED for owner review before Phase I, do not auto-advance.
- Phase I gate met (milestone): connected data mode is wired end to end (Tier 1 in full). Connected-mode grounding is built from `derived_signals` only (numeric signals per layer, a vector rendered as `vector[len]`, never raw text) and threaded as an optional parameter appended only on the connected path, so the outside-in prompts stay byte-for-byte unchanged (proven by a regression test). The connected refresh service runs boundary connectors in process, rejects declared-only connectors as "available, not connected", and persists only in the caller (open `connector_runs`, assert the set, delete-prior then insert `derived_signals` in one transaction so a refresh supersedes, close the run); persistence never lives inside a connector. `seedTenant` branches on `tenants.dataMode`: connected mode is refresh-only for an existing tenant (load the `ProfileOutput`, fail loudly if absent, ground layers on derived signals), and both modes run one shared `runLayers` helper. A new `edge_agents` table holds a per-tenant agent credential (scrypt hash only, active or revoked); the `/api/agent` routes (register, config pull, signal ingest) are tenant-scoped and gated by a bearer credential that is reloaded per request for immediate revoke and never trusts a proxy client-certificate header. A separate in-client `@workspace/edge-agent` package imports `@workspace/connectors` only, runs edge connectors, and posts a `DerivedSignalSet`; it is proven over a real mutual-TLS loopback handshake (a no-certificate client is rejected), and its base URL is enforced HTTPS by default (loopback or an explicit test opt-out aside). The runtime no-write guard wraps every extraction as a documented tripwire, with the static import-boundary test (no `node:fs`, no db root in connector or edge-agent source) as the primary control. No edge connector is implemented yet; every edge connector renders as available, not connected, and the runner is proven with an injected stub, not faked telemetry. Typecheck, build, and the full suite are green (connectors 27, cortex 55, db 8, scripts 4, api-server 96, edge-agent 10, portal 108); zero new npm deps; the long-dash sweep is zero across source and all 22 tables. Tier 2 (the split pipeline and `localModelAdapter`), Tier 3, and the portal connected-mode screens are deliberately deferred. The drift report is `phase-I.md`; the V2 build report appends the Phase I section. This is a milestone hard-stop: PAUSED for owner review before Tier 2, do not auto-advance.
- Phase J gate met (not a milestone, but gated): Tier 2 splits the cortex by sensitivity. In connected data mode the two Lens stages (perceive, hypothesise) run in-boundary on a local model seat through a single narrow seam (`ExtractionZoneRuntime` in `lib/cortex/src/stages/extractionZone.ts`), while the external Synthesist (narrate) and the adversarial seats (confound, challenge) plus the Evaluator and enrichment stay on their external models and receive only the profile, the in-boundary Lens output, and the math-only derived-signal grounding, never raw client content. The in-boundary adapter (`lib/cortex/src/clients/local.ts`) speaks the OpenAI-compatible `/v1/chat/completions` wire over the Node global fetch (no new dependency), with strict JSON mode, a Bearer only when an api key is set, 429 backoff, and one corrective retry; `getExtractionRuntime(env)` returns it when a local model is configured and null otherwise. A fourth provider `local` and `resolveLocalSeat(env)` read the model from `LOCAL_MODEL_*` env, so no model literal enters source and `SEATS` stays the three external seats (the config invariant holds). The outside_in path is byte-for-byte unchanged (the runners default to an outside_in `StageContext`, the external path is taken unchanged, proven by the grounding regression and an outside_in routing test). Fail-loud honesty per the architect's Option C: an unconfigured connected Lens returns "available, not connected" with no silent external fallback, proven by test. The TEE is deliberately not built; the seam is the deliverable, and a later confidential-computing runner implements the same interface with no stage or orchestrator change. Typecheck, build, and the full suite are green (cortex 66 including a real `node:http` adapter test and the injected-runtime split tests, api-server 96); zero new npm deps; the long-dash sweep is zero across source and all 22 data tables. One architect non-blocking note applied: the adapter no longer logs an upstream error body. Tier 3 (Phase K) and the portal connected-mode screens (Phase L) are deliberately not built. The drift report is `phase-J.md`. This is a gated stop: PAUSED for owner confirmation before Phase K, do not auto-advance.
- Phase K gate met (milestone): Tier 3 lands per-tenant cryptographic isolation, no standing human access, and a hash-chained provenance ledger, backend only (the portal security, break-glass, connections, and provenance screens are Phase L). Each signal value is sealed with a fresh AES-256-GCM data key (DEK) wrapped by a per-tenant 32-byte key (KEK) held in a local KMS, stored as an envelope `{v,alg,keyRef,iv,tag,ct,wrappedDek}` inside the existing `derived_signals.value` jsonb (no column churn); the derived-set root hash is still over the plaintext math. A swappable cloud/customer KMS adapter reports "available, not connected" until a customer key is configured, never a fabricated wrap. Crypto-shred is real: revoking a tenant key destroys the KEK material first and only then commits the revoked status and stamps `revokedAt` (a failed destroy throws before any status change, so a revoked key is never reported while its material survives), emits a structured revocation log line, and leaves the ciphertext inert; both reads require an active tenant key and verify each envelope's `keyRef` against it before decrypting, so both the human and machine reads then fail loud with a typed `crypto_shredded` error rather than any plaintext or empty grounding. No standing access: a human raw-signal read requires tenant access plus an active, unexpired, unrevoked break-glass grant for every role including owners, and every access appends an event to the new `access_grant_events` table; the in-boundary machine-grounding read is a separate service API, exempt by design, never a middleware bypass. The provenance ledger exposes only `appendEntry` and `verifyChain` (no update or delete), serializes per-tenant appends on a Postgres advisory lock, and links entries by `contentHash = sha256(canonical({tenantId, claimPath, sourceRef, prevHash}))` over source references only. outside_in is byte-for-byte unchanged (no key, no encryption, grounding regression still identical). Typecheck, build, and the full suite are green (123 tests across 15 files, up from 96; 27 new across crypto round-trip and tamper, key-lifecycle revoke failure-injection, ledger order and tamper, and the HTTP security surface); zero new npm deps (node:crypto, the pg already in the lockfile, global fetch, workspace packages); the long-dash sweep is zero across source and all 24 data tables (now including `kms_local_keys` and `access_grant_events`). Acceptance is proven by test, not UI, as the phase requires; the Playwright testing skill is for UI flows and explicitly not for API-only verification, so it does not apply to this backend-only phase. Logged drift: the default KMS is a software key store on operator-controlled Postgres (`kms_local_keys`), not a hardware security module, and the customer-managed-key path is declared and stubbed honestly until configured; database-role append-only on the ledger table (revoking UPDATE and DELETE) is a deployment-time hardening left to the operator, with the hash chain plus serialized append as the application-level integrity control. The drift report is `phase-K.md`; the V2 build report appends the Phase K section. This is a milestone hard-stop: PAUSED for owner review before Phase L, do not auto-advance.
- Phase L gate met (not a milestone, but gated): the portal security surfaces are built over the Phase K Tier 3 backend, changing no Tier 3 guarantee. A framework-free `lib/securityApi.ts` mirrors `adminApi.ts` with typed outcomes, a 401 mapped to an unauthorized signal, and the three Tier 3 refusals mapped to their own honest UI states (break_glass_required -> 403, crypto_shredded -> 409, signal_unreadable -> 422, branching on the response body code, never an empty list). An owner-only `/security` console (gated on `user.role === "provider-owner"`) carries four tabs behind a tenant gate so no panel spins on a null tenant: Posture (tenant key status, active KMS provider and connected state, the customer-managed KMS as "available, not connected", with provision and revoke), Connections (connection-security facts only, the existing feeds page left untouched), Break-glass administration (create time-boxed grants via the reused owner-only admin users route, list grants with the active versus expired versus revoked distinction, revoke, and the append-only access-event audit), and Provenance (verify intact or broken with length, broken index, and detail). A separate all-role `/break-glass` page reads the decrypted human signals under an active grant, with grant-required, crypto-shredded, unreadable, empty, and ready each rendered as its own state and raw values never cached or exported. One minimal backend addition: `GET /api/security/tenants/:id/key` now also returns `customerKms` from `customerKmsStatus()` so the UI does not invent the customer-KMS seam. Typecheck, build, and the full suite are green (382 tests; portal 144, up from 108 with 36 new in `securityApi.test.ts`); zero new npm deps; the long-dash sweep is zero across the Phase L source. e2e acceptance used the Playwright testing skill against a seeded throwaway provider-owner (the owner secrets are not in the testing runtime and only the scrypt hash is stored), driving all four console tabs and the all-role read to honest non-loading states and cleaning up the seeded row. Logged drift: built in-repo on the existing Phase E portal rather than as a fresh react-vite first build; the single `customerKms` backend field; the grant picker reuses the admin users route; the e2e seeded-owner login. The drift report is `phase-L.md`; the V2 build report appends the Phase L section. This is a gated stop: PAUSED for owner review before the next phase, do not auto-advance.
- Phase M gate met (not a milestone, but gated): the closing verification of the connector and SOC 2 stage (Phases H through L) against Part 8 of the addendum, plus the consolidated build-report append that Part 8 item 11 requires. Phase M built no product feature and changed no product code; its only artifacts are the documentation and a throwaway latency-measurement script that was deleted after the run. All eleven Part 8 items were checked: nine are met, item 2 is honestly partial (the catalogue is the full 46 connectors across all ten families and the registry returns an honest "available, not connected" for declared-only keys, but only the bring-your-own-warehouse family has the two connectors that run end to end, `generic-sql` and `redshift`; the other nine families are declared with correct layer and signal mapping but no runtime), and item 9 is met with a logged residual (provenance append-only is application-layer plus the hash chain plus the UI verify, while database-role-level write blocking is a deployment-time hardening that is not in place). The connected-refresh latency was measured on the real warehouse path (the `generic-sql` connector through `refreshConnectedTenant` against a real Postgres-wire warehouse, four aggregate measures fanned across fourteen layers into 56 encrypted derived-signal rows with a provenance root): median 60.9 ms, range 51.2 to 67.6 ms on local Postgres-wire, a floor that excludes client wide-area-network latency. Typecheck and build are green (exit 0); the full suite is green at 382 tests; the long-dash sweep is zero on both sides (the source guard and a row-cast sweep over all 24 public tables); zero new npm dependencies. The drift report is `phase-M.md`; the build report append is the Phase M section of `docs/build-report-v2.md`. This is a per-phase gated hard-stop that closes the connector and SOC 2 stage: PAUSED for owner review before the next stage, do not auto-advance.
- Phase N gate met (not a milestone, but gated): Stage 3 opens with cost and token observability. A new `model_usage` ledger records one row per REAL billed model call; a cortex pricing module (`lib/cortex/src/pricing.ts`) is the only place token counts become dollars, keyed by the three cortex seats via `SEATS` so no model literal enters source (the config invariant holds), with published list-price defaults the operator must verify (reasoner 3 in / 15 out, evaluator 1 in / 5 out, grounder 1.25 in / 10 out per MTok, web search 0.01 per call, self-hosted or unknown model at zero). Cost is computed from real token counts only, rounded to the six decimals of the `numeric(12,6)` column, never fabricated. The honesty pivot is an explicit `billed` flag on the stage telemetry: it marks a real token-billed response (a success OR a 200 that billed tokens then failed our schema validation), the usage writer records a row only when `billed && model`, so a no-call failure (no in-boundary model, a provider with no env, a transport failure before any response) carries `billed: false` and produces NO row and the ledger never holds a fabricated zero-cost line; each client also sums tokens across its two-attempt corrective retry so a billed-then-retried attempt counts once with the summed tokens. The orchestrator is the sole side-effect owner and taps usage in exactly three places (the stage run on both the ok and error path, the enrichment as one row not the batched folded peers, and the profile build after the tenant is ensured with run and layer null); resume paths return before the tap so a resumed run records no duplicate. A budget governor (`budget.ts`) enforces env-backed monthly caps (`SPEND_GLOBAL_MONTHLY_CAP_USD` default 1000, `SPEND_TENANT_MONTHLY_CAP_USD` default 50, `SPEND_ALERT_THRESHOLD` default 0.8) by reading the real summed ledger spend, refusing a new seed once a ceiling is reached and warning at the threshold, enforced in the seed and refresh routes with a clear typed HTTP error AND defensively in the seed path before any model spend (marking the tenant failed on a breach); the owner-only `priorityOverride` bypasses the GLOBAL ceiling only, never the per-tenant ceiling. An owner-only `GET /api/spend/summary` returns the month, totals, and breakdowns by tenant, seat, stage, run, and day plus the caps (member 403, unauthenticated 401, `numeric` cost as a JS number), rendered by a portal `/spend` console with honest loading, empty, error, and unauthorized states. Typecheck and build are green (exit 0); the full suite is green at 417 tests (api-server 139, portal 149, cortex 80, connectors 27, edge-agent 10, db 8, scripts 4), with the spend summary reconciling to a direct `SUM` over the ledger and the usage tests proving the one-row invariant, the no-call no-row case, the billed-but-failed real-cost case, and the retry token summation; the long-dash sweep is zero on both sides (the source guard and a per-row cast over the `model_usage` ledger and the run telemetry); zero new npm dependencies. A first architect `evaluate_task` FAILED with three findings (fabricated zero-cost rows on no-call failures, dropped tokens on billed-but-failed or retried calls, and the connected-tenant seed path skipping the budget assertion); all three are fixed and the re-review returned a PASS. The drift report is `phase-N.md`; the build report append is the Phase N section of `docs/build-report-v2.md`. This is a per-phase gated hard-stop: PAUSED for owner review before the next phase, do not auto-advance.
- Phase O gate met (not a milestone, but gated; run autonomously back to back with P and Q at owner authorization, so not paused here): connector operational reality, the unhappy path the clean extraction stage did not build. An alert SEAM (`lib/alerts/alerter.ts`, one `Alerter.emit` interface, a `DbAlerter` that records one pending `alert_events` row then logs only routing fields, an `AlertEvent` whose details are scalars only so no secret or raw record can attach) is the seam the Phase P notifier will consume; the type enum already declares all six alert kinds so the notifier needs no migration. OAuth token refresh (`oauthRefresh.ts`) renews every connected oauth2 connection inside its per-connector `oauthRefreshLeadSeconds` window, clearing prior error and rotating the credential ref on success, and on failure flips to error with `reauthentication_required` and a critical `oauth_refresh_failed` alert; there is no oauth2 runtime, so the default `NotImplementedTokenRefresher` rejects honestly and the scheduler is proven with an injected refresher, the failed-renewal path fully real. `startConnectorMaintenance` runs the loop from the server entrypoint only (never app.ts), no overlap, swallowed tick failure, unref'd timer. A per-connection token bucket (`rateLimiter.ts`) sized from the descriptor `quotaProfile` is enforced before each extraction; `runWithThrottleRetry` retries ONLY a typed `ConnectorThrottleError` up to `maxAttempts`, honoring `Retry-After` else exponential backoff capped at `maxRetryAfterSeconds`, while a genuine error propagates on first throw and is never retried (mirrors the seed 429 handling). Connector health (`connectionHealth.ts`) is derived at read time from real last-success and last-error timestamps and the staleness threshold (error, degraded, healthy; never-run reads degraded), never stored so it cannot drift, surfaced worst-first by the owner-only `GET /api/security/tenants/:id/connector-health` route (24h staleness fallback) and a new `ConnectorHealthSection` in both the Connections and Posture security panels with honest loading/empty/error/unauthorized states. The connected refresh (`connectedRefresh.ts`) takes a bucket token before extraction, wraps the guarded extraction in the throttle-retry so a throttled source backs off and RECOVERS without failing the run, records `lastSuccessAt` and resets to connected on success, and on failure flips to error with `rate_limited` or `extraction_failed` and emits a `connector_error_transition` alert ONLY on the transition into error (no re-alert while already error); both emissions are best-effort so they never mask the refresh failure. Incremental extraction is plumbed end to end (a `WatermarkValue` contract field, a `nextWatermark` returned with the derived set, the cursor passed only to a declaring connector, the watermark persisted in `tenant_connections.cursor_watermark` only when supported and a cursor came back, never source data) but every production descriptor keeps `incremental.supported = false` because the warehouse runtimes compute whole-table aggregates and an incremental aggregate would fabricate a number, so the seam is real and tested by temporarily enabling support and dormant in production (every refresh is a full derive, any cursor dropped). New table `alert_events`; new columns on `tenant_connections` (`last_success_at`, `token_expires_at`, `cursor_watermark`, `last_error_code`, `last_error_at`, `last_error_message`); new route the connector-health read; no new env (lead time, staleness, and quota are descriptor fields, the maintenance interval a code default). Typecheck and build green (exit 0); the full suite green at 441 tests (api-server 161, portal 149, cortex 80, connectors 29, edge-agent 10, db 8, scripts 4); the long-dash sweep zero on both sides (the source guard, and a per-row cast over every text and jsonb column in every public table including `alert_events`); zero new npm deps. The architect `evaluate_task` returned PASS on the first review with no severe findings and two non-blocking hardening notes deferred until a real incremental connector exists (runtime watermark-shape validation; sanitizing a persisted `lastErrorMessage` before broad UI display). The drift report is `phase-O.md`; the build report append is the Phase O section of `docs/build-report-v2.md`. Per the owner-authorized autonomous run this does not pause; it proceeds to Phase P.
- Phase P gate met (not a milestone, but gated; run autonomously back to back with O and Q at owner authorization, so not paused here): observability and alerting, the consuming half of the Phase O alert seam. A Sentry-compatible error reporter (`lib/observability/sentryReporter.ts`) parses a standard DSN and POSTs a hand-built Sentry envelope over the Node global fetch with no SDK; with no `SENTRY_DSN` the default is a no-op reporter ("available, not connected") so `captureError` is a silent no-op rather than a boot crash, the payload is an allowlist of scalar context only (subsystem, route, level, tenant id, run id) and never a request body, headers, raw connector records, or a secret, and `captureError` never throws into its caller so an observability outage can never alter or delay a real request. A pluggable notifier (`lib/alerts/notifier.ts`) drains pending `alert_events` rows with `FOR UPDATE SKIP LOCKED` inside a transaction (so concurrent ticks, even across instances, never deliver a row twice), formats each from routing fields and scalar details only, delivers through the env-selected transport (a Slack incoming webhook `SLACK_WEBHOOK_URL`, a generic JSON webhook `ALERT_WEBHOOK_URL`, or the honest default log sink), and marks the row `sent` or, on a terminal failure, `failed`; the drain loop runs from the server entrypoint only (never app.ts), never overlaps, swallows a tick failure, and unrefs its timer. Phase O already emitted `connector_error_transition`; Phase P wires the rest so all required classes deliver: the orchestrator `runLayer` catch emits `seed_run_failed` and calls `captureError`, `budget.ts` emits `budget_threshold` deduped by an entity id scoped to the cap kind and month (so a threshold alerts once per scope per month, not on every priced call), the security route emits `break_glass_used` after the access is appended to the audit, and the provenance verify route emits `provenance_integrity_failed` only when `verifyChain` returns `ok === false`. An owner-only `GET /api/operations` (behind requireAuth and requireOwner) returns, all from real tables, the in-flight runs with current stage, the recent failures with the failing stage, the live seed-queue depth read from the `pipeline_jobs` claim queue as running and waiting counts, and the recent alert feed; no synthesized metric. The health route is rewritten to a structured per-dependency status (database probed with a real round-trip, secret store through its status seam, both model providers reported `configured` or `not_configured` from env and escalated to a live reachable check only on an explicit deep probe `?deep=1` or `HEALTH_DEEP_CHECK=1` so a poll never silently bills a model call); a dependency that cannot be probed reads `unknown`, never a fabricated `ok`, and the overall status is the honest worst-of. No new table and no new column (the notifier advances the Phase O `alert_events.notification_status`); the new env is all optional with honest defaults (`SENTRY_DSN`, `SENTRY_RELEASE`, `SENTRY_TIMEOUT_MS`, `SLACK_WEBHOOK_URL`, `ALERT_WEBHOOK_URL`, `ALERT_NOTIFIER_TIMEOUT_MS`, `ALERT_DRAIN_INTERVAL_MS`, `HEALTH_DEEP_CHECK`). Typecheck and build green (exit 0); the full suite green at 464 tests (api-server 184 across 25 files, portal 149, cortex 80, connectors 29, edge-agent 10, db 8, scripts 4) with the Sentry reporter unit tests, the notifier integration tests (claim-and-deliver, exactly-once per row, log sink, terminal-failure marks the row failed), the health integration tests, the operations integration tests, the budget threshold integration test, and a `break_glass_used` assertion added to the security suite; the long-dash sweep zero on both sides (the source guard, and a per-row cast over every text and jsonb column in every public table); zero new npm deps (the Node global fetch, the existing pg, and workspace packages only). The architect `evaluate_task` returned PASS with two non-blocking production-hardening notes deferred until a real external sink and a multi-instance deployment exist (a delivery-side idempotency key so a crash between webhook POST and status commit cannot re-deliver, and a unique constraint on the budget threshold dedupe key so concurrent seeds cannot duplicate a threshold alert). The drift report is `phase-P.md`; the build report append is the Phase P section of `docs/build-report-v2.md`. Per the owner-authorized autonomous run this does not pause; it proceeds to Phase Q.
- Phase Q gate met (not a milestone, but gated; the final phase of the owner-authorized autonomous O-P-Q run, so execution stops for owner review after it): a secrets vault behind the existing `SecretStore` seam. A zero-SDK GCP Secret Manager adapter (`lib/secrets/gcpSecretStore.ts`) implements the full `get`/`set`/`delete` over the Secret Manager REST API with the Node global fetch: construction validates nothing so an unset project never crashes the boot, and the first call resolves the project and a token lazily and throws a precise "available, not connected: set GCP_PROJECT_ID to connect it" error when the project is missing; `get` reads `versions/latest:access` (404 to null, base64 decode), `set` creates the container (tolerating 409) then adds a base64 version, `delete` tolerates 404 idempotently; the token is the cached GCP metadata token or, with `GCP_SECRET_MANAGER_TOKEN_SOURCE=env`, `GCP_SECRET_MANAGER_ACCESS_TOKEN`; every ref is validated against the Secret Manager id grammar before any network call; every request is bounded by an `AbortController` timeout (`GCP_SECRET_MANAGER_TIMEOUT_MS`, default 5000); and no value, token, or response body is ever logged or attached to an error. `getSecretStore` selects the backend via `SECRET_STORE_PROVIDER` (unset or `env` uses the local env-backed store, `gcp` selects the adapter). Every runtime secret is resolved by name through the store, never from `process.env` at the call site: `SESSION_SECRET` via `requireSecret` (auth middleware, auth and admin routes, PIN pepper, connected-refresh token salt), `OWNER_PASSWORD` via `store.get` at bootstrap (only the scrypt hash is persisted), and connector credentials via `buildConnectorContext.resolveSecret`, which the warehouse connector uses to resolve its `scope.authRef` (`tenant_connections` stores only the reference). `tenant_keys.kmsKeyRef` is deliberately kept on the separate KMS boundary, not folded into the secret store, because the KEK has a different blast radius (architect-approved). Typecheck and build are green; the full suite is green at 478 tests (api-server 198 across 26 files, portal 149, cortex 80, connectors 29, edge-agent 10, db 8, scripts 4); the long-dash sweep is zero on both sides (source guard plus a per-row cast over every text and jsonb column in every public table). Acceptance is proven by `secretResolution.integration.test.ts`: a connection authenticates by resolving its `authRef` through the store, and a unique sentinel resolved through an injected store during a real refresh is swept for across every public text and jsonb column (catalogue-driven, non-empty-list guarded) and the repo-root `.replit`, with a total of zero. Logged drift: the KMS key ref stays on its own boundary; `EnvSecretStore.set`/`.delete` mutate the in-process environment only (honest by design, the durable write path is the GCP adapter); with the default store a secret reference is an env var name whose value lives in the platform secret store, the legitimate durable home, outside the "no value in a table or `.replit`" scope. Zero new npm dependencies. The drift report is `phase-Q.md`; the build report append is the Phase Q section of `docs/build-report-v2.md`. This gated hard-stop closes the autonomous O-P-Q run: PAUSED for owner review, do not auto-advance.
- Phase R gate met (not a milestone, but gated; run autonomously back to back with S and T at owner authorization, so not paused here): expand test coverage and confirm CI. The Operations prompt names this "introduce testing", but a Vitest suite and a GitHub Actions CI workflow have existed since Phase B, so the adaptation guide rescopes R to proving every load-bearing invariant has a test that turns red when broken and filling the one real gap. Seven of the eight invariants were already pinned: the DerivedSignalSet guard rejecting raw records, the connector and edge-agent extraction path holding no db handle and no `node:fs`, the four PIN failure modes returning one byte-identical error with a valid PIN succeeding and decrementing exactly once plus requireOwner refusing a member, the session cookie verifying valid and rejecting tampered and expired, the provenance ledger append-only with a broken chain detected, and the long-dash guard over authored source. The gap was prompt hygiene: `lib/cortex/src/prompts/promptHygiene.ts` is a pure unit-anchored detector (a figure is digits welded to a bps, percent, or dollar unit, with a greppable `PROMPT_HYGIENE_ALLOW_MARKER` escape hatch) and `promptHygiene.test.ts` walks the real prompt directory (excluding the test and the detector module, which hold the example strings on purpose), asserts the authored builders scan to zero, then proves the guard bites on synthetic figures while staying clean on placeholders and interpolation. No prompt source was altered to pass. CI (`.github/workflows/ci.yml`) runs typecheck, build, and test as separate required steps of the `verify` job and blocks the merge on any nonzero exit; the hosted runner cannot execute here, so the same steps run locally and pass. Typecheck and build green; full suite green at 482 tests (was 478; +4 prompt-hygiene), api-server 198, portal 149, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; long-dash sweep zero on both sides (source guard plus a database-wide cast over every text and jsonb column); zero new npm deps. The drift report is `phase-R.md`; the build report append is the Phase R section of `docs/build-report-v2.md`. Continuing to Phase S in the autonomous run; the hard stop is after the Phase T milestone.
- Phase S gate met (not a milestone, but gated; run autonomously back to back with R and T at owner authorization, so not paused here): retention and deletion. A new `retention_events` audit table (a `retention_action` enum of `ttl_purge` and `tenant_erasure`, set-null `tenantId` and `authorityUserId` so the audit outlives the tenant or operator it records, an `authorityRole`, a `scope` jsonb, a `deletedDerivedSignalCount` defaulting to zero, an optional `redactionLedgerEntryId` plain-uuid pointer that is deliberately not a foreign key so the ledger stays independent, a `reason`, and `createdAt`, with two read indexes) is pushed. The provenance ledger is refactored to expose `appendEntryTx`, the same advisory-locked tail-read-then-insert append composed inside a caller's transaction, with `appendEntry` now wrapping it; the surface stays append-only (no update, no delete), proven by the widened ledger surface test. `runRetentionPurge` deletes derived signals whose `computedAt` has fallen behind the `RETENTION_TTL_DAYS` (default 90) cutoff and writes one `ttl_purge` audit row per affected tenant, while a tick that purges nothing writes no row and logs nothing (honest empty state); `startRetentionPurge` (`RETENTION_PURGE_INTERVAL_MS`, default 6 hours) runs the loop from the server entrypoint only, never overlapping, swallowing a tick failure, with an unref'd timer, mirroring the connector-maintenance and notifier loops. `eraseTenantDerivedSignals` runs one transaction that deletes the tenant's derived signals, computes a `sha256` digest over the sorted erased ids, sorted provenance refs, count, and scope, appends a provenance redaction through `appendEntryTx` (`claimPath` `redaction:derived_signals:tenant`, `sourceRef` `sha256:<digest>`), and inserts a `tenant_erasure` audit row carrying the redaction entry id, so the erasure is atomic and `verifyChain` still passes because the ledger only grew. An owner-only router exposes `DELETE /api/retention/tenants/:id/derived-signals` (a `tokenRef` body is rejected with a 400 `token_erasure_not_supported_for_aggregate_signals` before any delete because aggregate signals have no identity thread, an unknown tenant is 404 `tenant_not_found`) and `GET /api/retention/tenants/:id/events`; the router is mounted in `app.ts` and the purge loop is started from `index.ts` only. Typecheck and build green; full suite green at 495 tests (was 482; +13 retention, 5 service and 8 route; the ledger surface test was widened to admit `appendEntryTx`), api-server 211, portal 149, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; long-dash sweep zero on both sides (source guard plus a database-wide cast over every text and jsonb column in every public table, now including `retention_events`); zero new npm deps. The architect `evaluate_task` returned PASS with no blocking issues. The drift report is `phase-S.md`; the build report append is the Phase S section of `docs/build-report-v2.md`. Per the owner-authorized autonomous run this does not pause; it proceeds to Phase T, the milestone.
- Phase T gate met (MILESTONE, hard stop; the third and final phase of the owner-authorized R-S-T run): client onboarding experience. The Operations prompt frames this as "add organizations", but orgs, the four roles, scoped PINs, and tenant fencing landed in Phase D, so T delivers the self-serve onboarding on that base. A new `/api/client` router (session-gated, client-admin only) mints client-viewer PINs whose scope is forced server-side to the caller's own org and the `client-viewer` role (a widening attempt is rejected loudly with `scope_org_forbidden` / `scope_role_forbidden`), lists only own-org viewer invites, and revokes only own-org viewer invites (a cross-org or non-viewer PIN is 404); a shared `mintInvitePin` helper backs both this route and the owner admin route so they cannot drift apart. The client-viewer is made strictly read-only: both action mutation routes in `tenants.ts` now 403 a client-viewer after the tenant-access check, and the break-glass human-signal read in `security.ts` now refuses any non-provider role before the grant check (closing the one client-reachable path to raw decrypted signals); the portal war room hides the commit and status controls for a viewer so no affordance silently fails. The client first run is `Onboarding.tsx` over a framework-free typed `clientApi.ts` (honest loading, empty, ready, error), and the rollout is documented in `docs/client-onboarding-runbook.md`. Logged milestone decision: a client-viewer sees diagnosis plus reasoning chain plus provenance for their own tenant and nothing that crosses the client boundary (not cost or spend, not connector internals, not another tenant, not break-glass, not action writes). Typecheck and build green; full suite green at 526 tests (api-server 227, portal 164, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; +31: 14 client route, 2 read-only proofs, 15 portal clientApi); long-dash sweep zero on both sides. Zero new npm deps. The architect `evaluate_task` returned PASS (the first review's HIGH on a writable client-viewer was fixed and re-verified). The drift report is `phase-T.md`; the V2 build report append is the Phase T section. This is the MILESTONE hard stop and the end of the R-S-T run: PAUSED for owner review, do not auto-advance.
- Phase U gate met (not a milestone, but gated; the second-to-last phase of Stage 3, run autonomously back to back with V and W under owner authorization, so not paused here): backups and disaster recovery. The platform owns durable Postgres storage and point-in-time recovery, so Phase U mirrors the Phase Q framing: `docs/backup-and-dr-runbook.md` documents the RPO, RTO, retention window, the logical-restore versus PITR distinction, and the operator restore procedure, and the application builds the parts it can make real. A crown-jewel logical backup (`crownJewels.ts`) serialises the five tables whose loss could not be recomputed (`derived_signals`, `provenance_ledger`, `users`, `invite_pins`, `tenant_keys` reference only) with `row_to_json`; `restoreCrownJewelsIntoScratch` rebuilds each into an isolated `scratch_restore_*` schema (`CREATE TABLE ... LIKE ... INCLUDING DEFAULTS INCLUDING CONSTRAINTS`, no FKs or indexes, so it can never collide with live data); `runRestoreDrill` exports, restores, verifies the per-table counts, re-walks every restored tenant chain FROM the restored scratch rows (not the in-memory bundle), then always drops the scratch schema even on failure. The provenance-ledger archive (`ledgerArchive.ts`) writes a canonical stable-order serialisation of the whole ledger plus a per-tenant chain manifest to durable object storage with a `sha256` over the content-only canonical bytes (so an unchanged ledger is skipped, not re-archived), re-verifies every chain at export, writes write-once where supported, and records exactly one `backup_events` row per run; `verifyLedgerArchiveObject` reads an object back and re-confirms the digest and re-walks each chain; an empty or unchanged ledger writes no object and no row (`skipped`). The archive store is "available, not connected" like `gcpSecretStore`: `ARCHIVE_STORE_PROVIDER` unset or `local` uses a local-fs store so the archive and restore cycle are provable on a laptop, `gcs` selects a zero-SDK GCS JSON-API adapter over the Node global fetch that validates nothing at construction and throws a precise "set GCS_ARCHIVE_BUCKET to connect it" on first use (object keys validated against a traversal-safe grammar, requests bounded by `GCS_ARCHIVE_TIMEOUT_MS` default 10000). `startBackupArchive` runs the archive loop from `index.ts` only (no overlap, swallowed tick failure, unref'd timer, cadence `BACKUP_ARCHIVE_INTERVAL_MS` default 12 hours), mirroring the retention and notifier loops. A new `backup_events` audit table mirrors `retention_events` (one row per run, none on a skip). Owner-only routes behind `requireAuth` and `requireOwner`: `POST /api/backups/ledger-archive` (trigger), `GET /api/backups/events` (audit), `GET /api/backups/status` (provider and connected state, cadence, last archive; never a credential, bucket, or path). Typecheck and build green; full suite green at 557 tests (api-server 258 across 32 files, portal 164, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; +31 across three new backup test files); long-dash sweep zero on both sides (source guard plus a database-wide cast over every text and jsonb column in every public table, now including `backup_events`); zero new npm deps. The architect `evaluate_task` returned PASS after the first review's one MEDIUM was fixed and re-verified (the restore drill verifies the chain from the restored scratch rows, not the in-memory bundle); the one LOW (skip-unchanged archive not globally serialised across processes) is accepted as logged drift because a concurrent duplicate only writes a content-identical object under a distinct write-once key and a second honest audit row, never weakening integrity. The drift report is `phase-U.md`; the build report append is the Phase U section of `docs/build-report-v2.md`. Per the owner-authorized U-V-W run this does not pause; it proceeds to Phase V, the Stage 3 closing verification. The hard stop is after Phase W, before milestone X.
- Phase V gate met (not a milestone, but gated; the closing phase of Stage 3, Operations and Hardening, run autonomously back to back with U and W under owner authorization, so not paused here): verification and the build-report append. Phase V built no product feature and changed no product code; mirroring Phase M, its only artifacts are the `phase-V.md` evidence matrix, the consolidated Phase V section of `docs/build-report-v2.md`, and the drift updates. All twelve points of section 9 of the Operations prompt are met or honestly accounted for: typecheck, build, and the full 557-test suite pass (api-server 258, portal 164, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; hosted CI is the standing environmental drift, run locally); the cost rows and the Spend reconciliation, the capped-tenant block, OAuth-expiry and throttle recovery with the dead-connector alert, the failed-seed-fires-exactly-one-notification drain, the no-secret-value sweep over every table and `.replit`, every Phase R invariant's red-on-break test, the TTL purge and the chain-preserving erasure, the client-viewer fencing, and the scratch-schema restore drill are each proven by the integration suite against live Postgres; the long-dash sweep is zero on both sides (the source guard and a database-wide cast over all 107 public text and jsonb columns). The honest integration-versus-live boundaries are marked in the matrix: live paid model seeds happened in Phases C and F and were not re-run; OAuth refresh is proven against an injected refresher because there is no oauth2 runtime; the external alert sinks and the durable cloud secret and archive backends are available-not-connected unless configured; and a full PITR-to-new-instance restore is the operator's documented platform procedure. Zero new npm deps. The architect `evaluate_task` returned PASS. The drift report is `phase-V.md`; the drift index and the rollup are updated to "A through V". Per the owner-authorized U-V-W run this does not pause; it proceeds to Phase W, the opening of Stage 4.
- Phase W gate met (not a milestone, but gated; the opening phase of Stage 4, Differentiation and Moat, the last phase of the owner-authorized autonomous U-V-W run, so execution STOPS for owner review after it before milestone X): the outcome loop and value realized. The track record becomes a graded history. `committed_actions` gains three nullable commit-time snapshot columns (`predicted_value_usd` numeric(14,2), `baseline_metric` numeric, `baseline_at` timestamptz) and a new `outcome_measurements` table (one row per real measurement: `actionId` FK cascade, `measuredAt`, `actualMetric`, `realizedValueUsd` numeric(14,2), `varianceVsPrediction` numeric(14,2), `basis`, `status`, `note`, `recordedBy` FK set-null) with two new enums `outcome_measurement_basis` (measured, modelled) and `outcome_measurement_status` (pending, on_track, realized, missed). Two pure modules carry the honesty: `predictedValue.ts` parses a dollar prediction from the action's impact string only when anchored by a `$` or `USD` token (a percentage, a margin-point figure, or prose yields null, never an invented number), and `outcomeMath.ts` derives status (missed only on a final measurement), variance, the latest-measurement-per-action de-duplication, the value-identified-versus-realized summary that reconciles against a direct database sum, and a simple hits-over-resolved calibration that returns a null score on an empty record (kept loose deliberately; milestone AJ supersedes it with a Brier-scored ledger). The commit route snapshots the prediction and, in connected mode, the named-signal baseline; the new provider-only `POST /api/tenants/:id/actions/:actionId/measurements` records a measurement with `basis=measured` only when a real finite scalar derived signal backs it (a missing or non-scalar signal is a loud `400 signal_not_found`, never a silent downgrade to modelled) and status and variance derived server-side, with `GET .../measurements` and `GET /api/tenants/:id/outcomes`. The portal elevates the existing Track Record surface (no business-performance hero exists in V2) with the value counter, the calibration trust badge with misses surfaced, and per-action realized/variance/basis. Typecheck and build green (portal 1743 modules, api-server bundled); full suite green at 593 tests (api-server 286 across 34 files, portal 172 across 14 files, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4; new `predictedValue`, `outcomeMath`, `outcomeApi`, and outcome-loop integration tests); long-dash sweep zero on both sides (source guard plus a database-wide cast over all 108 public text and jsonb columns, now including `outcome_measurements.note`); zero new npm deps. The architect `evaluate_task` returned PASS. The drift report is `phase-W.md`; the build report append is the Phase W section of `docs/build-report-v2.md`. Phase W is the last phase before milestone X (benchmarking): this is a HARD STOP for owner review, do not auto-advance into Phase X.
- Phase X gate met (security milestone): benchmarking and the data network effect, with privacy from the first line. A tenant turns one private figure into a cross-tenant benchmark WITHOUT exposing any other tenant's raw data or identity. New schema (a `benchmark_opt_in` boolean default false on `tenants`, plus `benchmark_consent_events`, `benchmark_cohorts`, `benchmark_stats`, `benchmark_events`, exported from a new `benchmarks` schema module) is built so the published benchmark tables hold NO raw values and NO tenant references at all: a `benchmark_cohorts` row is a population (segment key, normalized sector and revenue band, member count), a `benchmark_stats` row is a distribution over that population (p25, p50, p75, sample count, a `noised` flag), and the recompute audit `benchmark_events` is identity-free (action, cohort, stat and skipped-tenant counts, the configured min cohort, the authorizing user and role). The ONLY tenant-scoped audit path in the feature is `benchmark_consent_events` (a row per opt-in or opt-out with the authority and an optional reason). The recompute reads each opted-in tenant's decrypted scalar signals through the MACHINE grounding read extracted from the orchestrator path (never the break-glass human read); a per-tenant unreadable key is caught, SKIPPED, and counted, so one crypto-shredded tenant never fails the run. A k-anonymity floor (`BENCHMARK_MIN_COHORT`, default 5) suppresses any cohort too small to hide an individual, and a cohort in `[k, noiseBand)` (`BENCHMARK_NOISE_BAND`, default 20) is published with bounded noise tied to a fraction of the IQR and clamped to preserve `p25 <= p50 <= p75`, flagged `noised` and surfaced as "privacy protected" (disclosed, never fabricated). `runBenchmarkRecompute` is pure and supersedes the prior set; `startBenchmarkRecompute` runs from the server entrypoint only (no overlap, swallowed tick, unref'd timer, cadence `BENCHMARK_RECOMPUTE_INTERVAL_MS` default 12 hours), mirroring the retention, notifier, and backup loops. Routes: tenant-access `GET`/`POST /api/tenants/:id/benchmark-consent` (default-off, a client-viewer is 403, a consent change writes one audit row), the layer-detail read returns `cohortBenchmark | cohortLock` ALONGSIDE the modelled `peerBenchmark` (never a contributor list, peer id, or peer value; both null unless the requester is opted in and eligible), and owner-only `POST /api/benchmarks/recompute`, `GET /events`, `GET /status`. The portal renders the verified-cohort distribution band (p25/p50/p75 with the requester's own self marker, the sample count, a "Verified cohort" pill, the noised note), the honest below-k lock, and a default-off consent toggle, keeping the modelled `peerBenchmark` separate so the two bases are never conflated. Typecheck and build green; full suite green at 627 tests (api-server 315 across 37 files, portal 177 across 14 files, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4); long-dash sweep zero on both sides (source guard plus a database-wide cast over all 118 public text and jsonb columns, now including the benchmark tables and `benchmark_consent_events.reason`); zero new npm deps. The architect `evaluate_task` returned PASS with one non-blocking hardening note (the live cohort read could re-gate stale stat rows against the current min-cohort and noise config; logged as drift, not built, because the recompute always re-applies the current config and a stricter floor only makes the next recompute more conservative). The drift report is `phase-X.md`; the build report append is the Phase X section of `docs/build-report-v2.md`. Phase X is a SECURITY MILESTONE: this is a HARD STOP for owner review, do not auto-advance into Phase Y.
- Phase Y gate met (not a milestone, but gated; the opening phase of Stage 4, Differentiation and Moat, run autonomously back to back with Z, AA, AB, and AC under owner authorization, pausing at the Stage 4/5 boundary before Phase AD): the portfolio intelligence view. The "portfolio" org type already exists from Phase D, so Phase Y builds ONLY the experience over it and added no schema. One read (`GET /api/portfolio/summary`, under the shared `requireAuth` gate) assembles a ranked multi-company board, cross-portfolio gap patterns, and a drill-down into any bound tenant's full diagnosis. Scope is resolved server-side from the session alone, never from the client: a provider seat sees every tenant as a portfolio; a seat whose `orgType` is `portfolio` with an `orgId` sees only the tenants its org is bound to through `org_tenants`; every other seat (a client org, or a user with no org) is refused with `403 portfolio_only`, and a missing session is `401`. A portfolio caller can never reach a tenant outside its bindings because it never names a tenant; the binding set IS the query, reusing the Phase T access posture. An empty binding set is an honest empty board, not an error. The pure `portfolioMath` module ranks each tenant by realized-and-at-risk value and open-gap severity (worst and best to the top), counts open gaps by severity, rolls the per-tenant outcome summary (via the shared `computeOutcomeSummary`) into portfolio totals with NULLABLE dollar figures, and derives cross-portfolio gap patterns only from persisted tenant-layer gaps and only for a gap shared by at least two tenants. The auth payload now carries `orgType` (the middleware left-joins `orgs`; the auth routes return it via `orgTypeFor()`) purely as a navigation hint; the server still fences the data by the session binding, never by this field. The portal adds a framework-free `portfolioApi.ts` (a discriminated union mapping 401 and 403 to their own honest states), a `PortfolioPage` state machine (distinct loading/empty/ready/forbidden/error states, drill-down via `setCurrentId`), the mirrored portfolio types, the `/portfolio` route with NO client-side role gate (the server fences and the page renders an honest forbidden state), and a TopNav link shown only to a provider or portfolio seat. Every dollar figure is computed from persisted state or rendered as a dash through `formatUsd`, never a fabricated `$0`. Typecheck and build green; full suite green at 646 tests (api-server 334 across 39 files including the new `portfolioMath` unit tests and the portfolio integration tests, portal 177, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4); long-dash sweep zero on both sides (the source guard plus a database-wide cast over all 118 public text and jsonb columns, unchanged because Phase Y added no schema); zero new npm deps. The architect `evaluate_task` returned PASS, with one non-blocking item (no dedicated portal unit test for `PortfolioPage` / `portfolioApi`; the server integration tests carry the functional and authorization coverage). The drift report is `phase-Y.md`; the build report append is the Phase Y section of `docs/build-report-v2.md`. Per the owner-authorized Stage 4 run this does not pause here; continuing to Phase Z.
- Phase Z gate met (not a milestone, but gated; the second phase of Stage 4, Differentiation and Moat, run autonomously back to back with Y, AA, AB, and AC under owner authorization, pausing at the Stage 4/5 boundary before Phase AD): proactive push intelligence. The persisted outcome loop becomes recorded, ranked, deliverable notifications, a deliberately SEPARATE seam from the Phase O/P operational alert seam (`alert_events`) with its own new enums so the two lifecycles never entangle. Two new tables (`lib/db/src/schema/pushIntelligence.ts`): `push_rules` is per-user, per-tenant, per-kind (`ownerUserId` NOT NULL, unique on `(ownerUserId, tenantId, type)`), carrying `enabled`, a `mutedUntil`, optional `minImpactUsd` / `minConfidence` floors (null means no floor), and a `channel`, with a default rule (enabled, no floor, in_app) materialized lazily per reachable tenant; `push_events` is one recorded notification, idempotent by `(ruleId, dedupeKey)` so the same breach in the same state never notifies twice and a state change mints a new event, with owner and tenant denormalized for a single-table inbox. Three new enums (`push_rule_type` of `outcome_shortfall` and `high_value_action`, `push_channel` of `in_app`/`slack`/`email`, `push_delivery_status` of `pending`/`suppressed`/`sent`/`failed`). The evaluator (`pushEvaluator.ts`) materializes the default rules, builds candidate breaches from real rows only (a `high_value_action` is an open committed action carrying a parsed dollar prediction; an `outcome_shortfall` is the latest measurement graded `missed` with a positive dollar shortfall), scores each (`rankScore` is `impactUsd * confidence / 100`), and records a `pending` or `suppressed` event with ON CONFLICT DO NOTHING; a disabled rule produces nothing, a muted rule still evaluates but records suppressed so a mute loses no record. The drainer (`pushNotifier.ts`) mirrors the Phase P notifier: it claims pending rows FOR UPDATE SKIP LOCKED, groups them per (recipient, channel), delivers exactly one ranked digest per group (capped at `PUSH_DIGEST_LIMIT`, default 10), and flips each claimed row to `sent` or `failed` exactly once; `in_app` is a no-op that always succeeds, `slack` reuses `SLACK_WEBHOOK_URL`, `email` is an available-not-connected adapter that fails loudly when `EMAIL_PUSH_ENDPOINT` is unset rather than dropping a notification. The scheduled Morning Brief (`pushBrief.ts`, `startPushMorningBrief`, cadence `PUSH_MORNING_BRIEF_INTERVAL_MS`, default 12 hours) runs the evaluation then the drain, started ONLY from the entrypoint, no overlap, swallowed tick, unref'd timer. Routes (`/api/push` under `requireAuth`): `GET /notifications`, `POST /notifications/:id/read`, `POST /notifications/read-all`, `GET /rules`, `PATCH /rules/:id`, `POST /rules/:id/mute`, every read and write fenced per-user and per-tenant. The portal adds `pushApi.ts` (honest discriminated states plus a pub/sub refreshing the nav badge), `NotificationsPage` (distinct loading/empty/ready/error states, read, read-all, tune, mute), the `/notifications` route with no client-side role gate (the server fences), and the `TopNav` NavBell unread badge. Every event figure is computed or null (`rankScore` zero when unquantified, so a figureless event ranks last and is suppressed, never a fabricated `$0`). Typecheck and build green (portal 1748 modules, api-server bundled); full suite green at 685 tests (api-server 357 across 41 files, portal 193 across 15 files, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4); long-dash sweep zero on both sides (source guard plus a database-wide cast over all 123 public text and jsonb columns, now including the `push_rules` and `push_events` text columns); zero new npm deps. The architect `evaluate_task` first returned FAIL on a broken-access-control finding (a push rule whose tenant binding was revoked after creation could still mint events AND have them delivered to an external channel, leaking a tenant's intelligence to a recipient who could no longer read it); the fix closed BOTH paths (the evaluator fences its loaded rules to the pairs reachable right now via `accessPairKey`; the drainer re-verifies access at delivery via `resolveAccessiblePairsForUsers` and fails revoked rows in place without handing them to a transport) with a self-contained revocation integration test as the regression guard, after which `evaluate_task` returned PASS. The drift report is `phase-Z.md`; the build report append is the Phase Z section of `docs/build-report-v2.md`. Per the owner-authorized Stage 4 run this does not pause here; continuing to Phase AA.
- Phase AA gate met (not a milestone, but gated; the third phase of Stage 4, Differentiation and Moat, run autonomously back to back with Y, Z, AB, and AC under owner authorization, pausing at the Stage 4/5 boundary before Phase AD): interactive challenge. A seat can CHALLENGE one finding in a layer's diagnosis, and the objection is re-reasoned through the Confounder and Synthesist seats, which either uphold the finding with reasoning or revise it with a new confidence and a note, every exchange recorded as an append-only auditable verdict. One new table (`lib/db/src/schema/findingChallenges.ts`): `finding_challenges` is one recorded challenge against one finding (the tenant, layer key, `findingRef`, a `findingHashRef` that is the sha256 of the canonical finding text AT challenge time so a later refresh is detectable, the challenger, the sanitized challenge text, the original confidence and basis snapshot, the revised confidence and `revisedBasis`, the confounder note, the synthesist reasoning, an honest `error`, the billed `telemetry`, and the `provenanceContentHash` a success appended). Two new enums (`finding_challenge_status` of `completed`/`failed`, `finding_challenge_outcome` of `upheld`/`revised`, null on a failed row). New cortex stages re-test the objection (challenge-confound) and decide uphold-or-revise (challenge-synthesis), both grounded, no model literal in source. The service `runFindingChallenge` (`artifacts/api-server/src/lib/challenge/findingChallenge.ts`) loads the live finding, runs the two seats, records each seat's billed usage, and writes the outcome in ONE transaction: on success it appends exactly one hash-chained provenance entry (claim path `<layerKey>.challenge.<findingRef>`, source ref a `challenge:sha256:` digest over the outcome with the user text HASHED in, never embedded) and inserts a `completed` row; on a model failure or a `revised` verdict with no new confidence it inserts an honest `failed` row with no provenance, never a fabricated verdict. The user's input is context, never an override: a challenge can never delete a finding, and a revise sets `revisedBasis` to `modelled_user_informed` on the challenge ROW only, never the cortex basis enum or the stored layer content. `listFindingChallenges` annotates each row with the challenger's email (null when removed) and an honest `isCurrentVersion` flag (true when the finding still hashes the same, false when a refresh changed it, null when it no longer exists). The routes (under `requireAuth` and `requireTenantAccess`) are `POST /tenants/:id/layers/:key/challenges` (the tenant fence, then a zod parse that bounds and TRIMS the text and refuses a malformed ref or a blank body, then the client-viewer model-spend gate, then the live re-reasoning, so a malformed, blank, or read-only request is rejected BEFORE any model call) and `GET /tenants/:id/challenges`. The portal EXTENDS Ask Different Day and the layer finding cards (`challengeApi.ts` honest discriminated states and `groupChallengesByRef`, a `ChallengeControl` with an inline box and per-finding history where a seat that cannot challenge sees history but no submit box, `FindingChallengeSlot` wired into the Causes/Actions/Challengers cards, the layer page fetching challenges alongside the layer and computing `canChallenge` as a non-client-viewer seat, and the Ask Different Day page grouping challenges by layer), never replacing the perspective lens. Typecheck and build green (portal 1750 modules, api-server bundled); full suite green at 716 tests (api-server 377 across 42 files including the new `findingChallenge` pure-helper unit tests and the challenge route-boundary tests, portal 204 across 16 files including the new `challengeApi` tests, cortex 84, connectors 29, edge-agent 10, db 8, scripts 4); long-dash sweep zero on both sides (the source guard plus a database-wide cast over all 135 public text and jsonb columns, now including the `finding_challenges` text columns); zero new npm deps. The architect `evaluate_task` returned PASS with no high or severe finding; two LOW items were fixed rather than logged as accepted drift (the submit response now returns the same serialized contract the history does, with this seat's email and the current-version flag, so a just-recorded challenge is never mislabelled as "a removed user"; and the challenge text is trimmed and refused when empty after trim so a blank submission never spends a model call). The remaining accepted LOW is that the challenge-history fetch is treated as non-critical supplementary data (a fetch failure renders the diagnosis without the overlay rather than blanking the page). The drift report is `phase-AA.md`; the build report append is the Phase AA section of `docs/build-report-v2.md`. Per the owner-authorized Stage 4 run this does not pause here; continuing to Phase AB.
- Phase AB gate met (not a milestone, but gated; the fourth phase of Stage 4, Differentiation and Moat, run autonomously back to back with Y, Z, AA, and AC under owner authorization, pausing at the Stage 4/5 boundary before Phase AD): the sellability pack. A finished diagnosis becomes a selling surface, additive and read-only, with no rewrite of any finding. One new table `diagnosis_share_tokens` (one privacy enum `diagnosis_share_privacy` of `summary_only`) holds a read-only share of one tenant's diagnosis: the opaque token is 32 bytes of CSPRNG entropy base64url, returned to the minter EXACTLY ONCE, and only its sha256 hash ever touches a column, so a database read can never reconstruct a working link. The data layer mints (clamping the lifetime to a 1-to-365-day band, default 30), lists shares as metadata only with a status derived from real columns (revoked, expired, active), revokes early and idempotently, and resolves a presented token by hashing it, loading the one unexpired unrevoked row, recording real access telemetry, and returning only the tenant id and privacy posture (a non-match is indistinguishable, keeping the public 404 uniform). The case-study builder produces anonymized, segment-level social proof from the real Phase W outcome loop: a DISTRIBUTION over a cohort of opted-in tenants in one segment (never a named company), reusing the EXACT Phase X k-anonymity floor and bounded-noise machinery, with the same `computeOutcomeSummary` the `/outcomes` endpoint uses, and only a tenant with a resolved outcome contributes; no tenant id, name, url, or date is ever emitted. The narrate stage gains a deterministic editorial voice-quality MEASUREMENT (`evaluateNarrativeVoice` in `lib/cortex`, seven genuine checks scored 0-to-100 with a band and per-check detail) that is recorded on the layer and never edits the prose, so a below-bar layer is shown at its real band. The authed provider/owner routes mint, list, and revoke tokens and read case studies; the ONLY unauthenticated data surface is `GET /api/public/diagnosis/:token`, which resolves the token, loads the overview, narrows each layer through `toPublicDiagnosisLayer` (stripping the owner persona, the diagnostic question, and the layer feed graph), and returns the public layers, the case study, and the powered-by mark behind a per-IP rate limit. The portal mounts the public page OUTSIDE the auth provider so a cold prospect never triggers an auth probe, with honest distinct states, and the Board Pack gains a provider-only panel to create a link, copy the full URL once, and revoke. Typecheck and build green; the full suite green at 758 tests (api-server 393, portal 225, cortex 89, connectors 29, edge-agent 10, db 8, scripts 4; +42: redactRoute, overviewProjection, shareTokens, caseStudies on the server, sellabilityApi and publicApi on the portal, voice in cortex); long-dash sweep zero on both sides over all 138 public text and jsonb columns (now including the `diagnosis_share_tokens` text columns); zero new npm deps. The architect `evaluate_task` first FAILED on one HIGH: the global error handler attached `req.path` to the observability context, and for the public route the path carries the bearer share token, so a failure deep in a public request could forward a live or attempted token to an external Sentry-compatible sink even though the database stores only the hash. The fix is a single redaction chokepoint (`redactRoute`) that collapses `/api/public/diagnosis/<bearer>` to the route template before any capture, with a regression test proving the token substring never survives; the re-review returned PASS. The accepted non-blocking drift is that the tenant case study is recomputed per public hit rather than cached (correct and never stale, a latency consideration at scale). The drift report is `phase-AB.md`; the build report append is the Phase AB section of `docs/build-report-v2.md`. Per the owner-authorized Stage 4 run this does not pause here; execution continues to Phase AC, which closes Stage 4, after which the run pauses at the Stage 4/5 boundary for owner review before Phase AD.
- Phase AC gate met (not a milestone, but gated; the closing phase of Stage 4, Differentiation and Moat, run back to back with Y, Z, AA, and AB under owner authorization): verification and the build-report append. Phase AC built no product feature and changed no product code; like Phase M closed Stage 2 and Phase V closed Stage 3, its only artifacts are the Stage 4 evidence matrix (`phase-AC.md`), the Phase AC section of `docs/build-report-v2.md`, and these drift updates. Each Stage 4 acceptance criterion is mapped to existing tested evidence with the proof type marked honestly: the portfolio ranked board and the access fence with a 403 outside the portfolio (`routes/portfolio.integration.test.ts`, `lib/portfolio/portfolioMath.test.ts`); the push ranking and low-impact suppression and the exactly-once Morning Brief drain and the access-revoked event failed in place (`routes/push.integration.test.ts`, `lib/push/pushMath.test.ts`, portal `lib/pushApi.test.ts`); the challenge finding-version helpers and the route boundary (`lib/challenge/findingChallenge.test.ts`, the Phase AA challenge block in `routes/tenants.integration.test.ts`, portal `lib/challengeApi.test.ts`), with the `runFindingChallenge` re-reason engine itself source-reviewed because it spends real model calls the suite does not run; the benchmark k-anonymity and bounded noise and identity-free recompute (`lib/benchmarks/benchmarkMath.test.ts`, `lib/benchmarks/benchmarks.integration.test.ts`, `routes/benchmarks.integration.test.ts`); the share-token one-way hash and clamp and status helpers (`lib/sellability/shareTokens.test.ts`), with the CSPRNG mint and resolve and the unauthenticated public cold-URL route source-reviewed (no `routes/public` or `routes/sellability` integration test); the public projection that strips the owner persona and diagnostic question and layer feed graph in the type and at runtime (`lib/overview/overviewProjection.test.ts`); the redaction chokepoint that keeps a bearer share token out of the observability path (`lib/observability/redactRoute.test.ts`); the anonymized k-anonymized case-study aggregation (`lib/sellability/caseStudies.test.ts`), with the loader's reuse of `computeOutcomeSummary` source-reviewed; and the deterministic voice measurement that never edits the prose (`lib/cortex/src/quality/voice.test.ts`). The global gates were re-run fresh: typecheck and build green, the full suite green at 758 tests (api-server 393, portal 225, cortex 89, connectors 29, edge-agent 10, db 8, scripts 4), and the long-dash sweep zero on both sides over all 138 public text and jsonb columns across 37 tables. Zero new npm deps. Two boundaries are honestly not live (the external push sinks and the durable secret and archive backends are available-not-connected unless configured; the realized-value and benchmark figures were produced by earlier real runs and recomputed, not by a fresh paid seed). The one accepted LOW logged as drift is that those source-reviewed write and IO paths (the challenge re-reason engine, the share-token mint and resolve, the unauthenticated public diagnosis route) lack a dedicated automated test; the architect marked it non-blocking and a future phase can close it with an injected-model challenge test and a public-route integration test. The architect `evaluate_task` returned PASS. Phase AC closes Stage 4; per the owner-authorized Y-Z-AA-AB-AC run the build now PAUSES at the Stage 4/5 boundary for owner review before Phase AD and does not auto-advance, and the next protocol milestone hard stop is Phase AI at the end of Stage 5.
- Phase AD gate met (not a milestone, but gated; the opening phase of Stage 5, Platform completion, run as a single owner-authorized phase): the full-application experience audit. Per the binding Adaptation Guide, Phase AD was RETIRED AS AN OVERHAUL and run instead as a SHORT audit of the existing portal against the design language (`docs/design-language.md`) and the AD acceptance set, FIXING DRIFT rather than redesigning. The phase is presentation-only: it changed CSS, shared page-chrome classes, and text-color token USAGE, and it reconciled the design-language doc to the implementation; it added no product feature and changed no route, schema, contract, or product logic, added and changed no test, and added zero npm deps. Two of the seven acceptance items carried real drift, both fixed at the SHARED level (inline styles outrank classes on specificity, so the fix converts shared chrome to classes plus one media layer rather than per-page rewrites): the CRITICAL 375px usability drift (new `.page-width`, `.top-nav-row`, `.top-nav-bar`, `.table-scroll` classes plus an `@media (max-width: 480px)` block; the three core read pages wrap wide tables in `.table-scroll`; desktop visually equivalent), and the WCAG AA contrast drift (a tone-INK mapping `toneInkVar`/`heroToneInkVar` routes every normal-sized under-24px tone text off the base brand hues to the ink shades, which clear 4.5:1 on paper, cream, and faint fills by a deterministic Node calc; base hue kept only for large 24px-plus figures, strokes, borders, icons, fills, dots, and dark surfaces; a global navy-soft `:focus-visible` ring added). The other five needed no code fix: two-click diagnosis and sub-five-minute first insight confirmed by source review, distinct loading/empty/error states audited across every async surface through the shared `DataState` with no fabricated data (audited, no fix), the design-language doc reconciled in three places (ink shades and responsive/accessibility guidance, gold/eyebrow guidance, navy-soft focus ring) with no unstyled default component found, and the regression contract held by the unchanged green suite. The global gates were re-run fresh: typecheck and build green (portal 1753 modules, api-server bundled), the full suite green at 758 tests (unchanged from AC; no test added or changed), and the long-dash sweep zero on both sides over all 138 public text and jsonb columns across 37 tables. Zero new npm deps. Accepted LOWs logged as drift: the 375px proof is source-reviewed (responsive class and media-query source plus page markup), not a live-viewport capture; the operator and admin tables outside the core-read scope (Portfolio, Spend, Break-glass, admin, Onboarding) are not retrofitted though `.table-scroll` is available; the Dashboard living-spec hex swatches and a few dark-surface translucent scrims are deliberate; and the Stage 4 per-hit case-study recompute on the public cold link is carried unchanged. The architect `evaluate_task` returned PASS after two remediation rounds. Phase AD opens Stage 5; per the owner authorization for this single phase the build now PAUSES at the AD gate for owner review before Phase AE and does not auto-advance, and the next protocol milestone hard stop is Phase AI at the end of Stage 5.
- Phase AE gate met (not a milestone, but gated; the ingestion stage of Stage 5, Platform completion, run under the owner-authorized AE-through-AI sequence that pauses only at the Phase AI milestone): five inbound data paths on ONE shared derive-and-discard core. The core (`lib/ingestion/ingestCore.ts`) parses inbound bytes or payload in memory, derives a `DerivedSignalSet`, persists ONLY the derived math through the Phase H connector terminus `persistDerivedSignalSet` (per-tenant encrypted, set root-hashed), guards every signal's key/window/unit as a non-identifying metric token at this terminus, appends one provenance entry per layer whose claim path records the ingestion method and layer (the source ref is the derived-set root hash over the math only), and discards the raw input; there is no raw-data store, no raw column, and no temporary raw file kept after processing. The paths: (1) ingestion API `POST /v1/ingest` gated by a per-tenant key in `ingestion_keys` (scrypt hash only, shown once at mint, revocable, equal-time miss path), rate limited, with an OpenAPI document; (2) per-source webhooks verified with a timing-safe HMAC against a secret in `webhook_sources`; (3) manual upload (csv and xlsx deterministic numeric math under generic positional `column_<n>` keys so a raw header never lands in a stored key; pdf and docx contract text extracted in the in-boundary seat and discarded, leaving only numeric metrics; strict MIME, extension, and size gating; honest derived-versus-discarded account); (4) SFTP drop (per-tenant credential, inbound-directory watcher, delete whether processed or rejected so no raw file lingers, quiet-period guard); and (5) an MCP server (`submit_signals` plus `get_diagnosis`, `get_layer`, `get_actions` under per-tenant auth). The portal Access console gains an ingestion panel to mint and revoke ingestion keys and webhook sources with a one-shot reveal. The central acceptance is test-proven: `routes/rawAbsence.integration.test.ts` drives all five paths with one unique sentinel and sweeps every public text and jsonb column plus the SFTP scratch directory, asserting the sentinel appears nowhere. Typecheck and build green; the full suite green at 794 tests (api-server 429 across 52 files, portal 225, cortex 89, connectors 29, edge-agent 10, db 8, scripts 4; +36 all in the six new api-server files: ingest 6, webhooks 5, upload 9, sftpDrop 5, mcp 10, rawAbsence 1); long-dash sweep zero on both sides over all 143 public text and jsonb columns across 39 base tables (now including `ingestion_keys` and `webhook_sources`); zero new npm deps. Two shared test-infrastructure faults surfaced by the sixth DB-touching integration file were fixed: a real SFTP quiet-period age bug (integer-versus-sub-millisecond comparison that could skip a fresh file, clamped at zero) and intermittent pool-timeout 500s under the concurrent cross-package suite (the root `test` script now serializes per-package runs and `lib/db` caps the per-process pool small under VITEST; the server default is unchanged). Accepted LOW: the portal ingestion admin client is source-reviewed (no new portal test); the server endpoints behind it are integration-tested. The architect `evaluate_task` returned PASS. The drift report is `phase-AE.md`; the build report appends the Phase AE section. Per the owner-authorized AE-through-AI Stage 5 sequence this phase does NOT pause at its own gate; execution continues to Phase AF (local LLM seat and sovereign mode), whose acceptance needs a real local OpenAI-compatible model endpoint absent from this container, so a `docs/drift/STOP.md` and pause follow at the AF gate if none is provided. The next protocol milestone hard stop is Phase AI at the end of Stage 5.
- Phase AF gate met (not a milestone, but gated; the local LLM seat and sovereign-mode phase of Stage 5, Platform completion, run under the owner-authorized AE-through-AI sequence): one local OpenAI-compatible seat and a single sovereign data mode that runs EVERY cortex stage in-boundary. Config gains a "sovereign" `CortexDataMode` resolved by a single `resolveCortexDataMode(env)` switch (`CORTEX_DATA_MODE=sovereign`) and a `runsOnLocal(stage, dataMode)` predicate (`sovereign || runsInBoundary`); `IN_BOUNDARY_STAGES` and the outside_in and connected behaviour are byte-for-byte unchanged, and the no-literal-model invariant holds (the local model id stays a SEAT in `lib/cortex/src/config.ts`, never inlined). In sovereign mode the orchestrator routes every stage through the local `ExtractionZoneRuntime` seam on one threaded `StageContext`; confound and challenge still RUN but on the local seat with grounding DROPPED (honest, no faked Google Search), and `seedTenant` uses a pure no-fetch homepage context so even the profile stays in-boundary. Sovereign-only telemetry markers (`executionMode:"sovereign"`, `groundingAvailable:false`, `webSearchAvailable:false`) are recorded only from a real run, a narrate/score verified-to-modelled calibration is applied before persistence, and a sovereign run that emits `verified_claims` fails loud rather than presenting a faked verification channel. The portal `ReasoningStrip` shows "Reasoned in sovereign mode" and "External grounding unavailable" when any stage telemetry is sovereign, with real model and token figures only and never a search or verified badge unless actually recorded. Proven HERMETICALLY (no live model) by an in-process conformance server and spies: connected makes zero frontier calls from the extraction zone, and sovereign makes zero external Anthropic/Gemini calls anywhere with every stage on the injected local runtime and confound/challenge not skipped. Typecheck and build green; full suite green at 819 tests (api-server 433 across 53 files, portal 225, cortex 110 across 13, connectors 29, edge-agent 10, db 8, scripts 4; +25 from AE: cortex sovereign-pipeline 10, calibration 7, and four added to homepageContext during remediation, plus four api-server reduceDecision tests added during remediation); long-dash sweep zero on both sides over all 143 public text and jsonb columns across 39 base tables (no schema added); zero new npm deps. The architect `evaluate_task` returned PASS after two remediation rounds (round one: keep the express reduction off in sovereign mode via a pure `reduceDecision`, label the narrate generator from telemetry not config, thread a calibrate seam into `executeStage`; round two: actually apply that calibrate before persist, replace the sovereign homepage fetch with the no-fetch context, and carry the real model id and sovereign markers through the folded enrichment telemetry; all applied and the gate re-run green before PASS). The drift report is `phase-AF.md`; the build report appends the Phase AF section. Per the real-endpoint blocker, Phase AF PAUSES at its own gate: the container has no local OpenAI-compatible model endpoint (`LOCAL_MODEL_*` unset, nothing listening, no GPU), so a `docs/drift/STOP.md` records what is proven hermetically versus what needs a real endpoint (real extraction quality and a local-only full seed with real latency and token/cost telemetry, plus the owner rerun steps and missing env), and the build does NOT auto-advance to Phase AG without an owner. The next protocol milestone hard stop is Phase AI at the end of Stage 5.
- Phase AG gate met (not a milestone, but gated; the curated custom-layer creation flow of Stage 5, Platform completion, run under the owner-authorized AE-through-AI sequence that pauses only at the Phase AI milestone): the `layers` table (the single source of layer identity; no `LAYER_KEYS` constant anywhere) gains an owner-gated lifecycle. A new `runnableLayerCondition()` predicate (canonical OR `approvedAt` set) is the SINGLE gate shared by both the seed fan-out (`orchestrator.loadRegistry`) and the portal catalog (`GET /layers`), so an unapproved custom layer is withheld identically from per-tenant output and the catalog and the two can never disagree. The schema gains an approval gate (`approvedAt`, `approvedBy`) and an optional `benchmarkCanonicalKey` self-reference (no new table). An owner creates a custom layer from a `.strict()` guarded template (exactly four metric tiles, an archetype the portal hero registry can render, at least one feed) that cannot smuggle `isCanonical`, `approvedAt`, or `sortOrder`, with honest valid-but-empty defaults and `deepStripDashes` at the new owner-supplied text sink; it persists UNAPPROVED (`isCanonical=false`, `approvedAt=null`) and runs nowhere until the owner approves it (owner-only, idempotent, records `approvedBy`, distinct errors for a canonical or a missing key). A supplied `benchmarkCanonicalKey` must reference an existing canonical layer, and the benchmark recompute excludes an unmapped custom layer from every cohort and pools a mapped one UNDER its canonical key, so cohort membership is never fabricated. `ALLOWED_ARCHETYPES` (api-server) and the portal hero `REGISTRY` keys (exported as `ARCHETYPE_KEYS`) are kept in lockstep by a source-reading guard test, since no package can be shared without a new dependency (currently nine renderable archetypes). The owner-only Access console gains a "layers" tab (`CustomLayerPanel`) with distinct honest loading, empty, and error states; the `adminApi` client gains typed loaders and writes. Typecheck, build, and the full suite are green (853 tests; api-server 458 across 56 files, portal 234 across 18, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +34 from AF: customLayer 15, customLayer.archetypeSync 1, layers.integration 8, a benchmarks guardrail, and adminApi +9); zero new npm deps; the long-dash sweep is zero on both sides over all 144 public text and jsonb columns across 39 base tables (one text column added, `benchmarkCanonicalKey`). The portal panel is source-reviewed (the one accepted LOW; its client functions and routes ARE tested). The architect `evaluate_task` returned PASS on the first pass with no findings. The drift report is `phase-AG.md`; the build report append is the Phase AG section of `docs/build-report-v2.md`. Per the owner-authorized AE-through-AI sequence Phase AG does NOT pause at its own gate; execution continues to Phase AH. The next protocol milestone hard stop is Phase AI at the end of Stage 5.
- Phase AH gate met (not a milestone, but gated; the cloud-portability phase of Stage 5, Platform completion, run under the owner-authorized AE-through-AI sequence that pauses only at the Phase AI milestone): a second cloud target for each "available, not connected" seam, a multi-instance queue proof, and the deploy artifacts, with zero new dependencies (node:crypto and the Node global fetch only, no AWS SDK). One shared zero-dependency AWS SigV4 signer (`lib/aws/sigv4.ts`) is pinned by AWS's published IAM ListUsers vector plus per-rule canonicalization properties (single-encode for s3 and double-encode for other services, sorted query, sorted and trimmed signed headers, host and x-amz-date with an optional x-amz-security-token, payload sha256). An AWS Secrets Manager adapter (`lib/secrets/awsSecretsManagerSecretStore.ts`) mirrors the GCP one exactly: available-not-connected until a region is set, the SAME `[A-Za-z0-9_-]{1,255}` ref grammar so a reference stays byte-identical across providers, the full Get/Create/Put/Delete surface over the signer and fetch, ResourceNotFoundException to null and idempotent delete, no value or token or body ever logged, selected by `SECRET_STORE_PROVIDER=aws`. An S3 archive adapter (`lib/backups/s3ArchiveStore.ts`) mirrors the GCS one: available-not-connected until `S3_ARCHIVE_BUCKET` is set, put/get/list/describe (describe non-secret), write-once via `If-None-Match: *` returning 412, selected by `ARCHIVE_STORE_PROVIDER=s3`. A new integration test proves the `pipeline_jobs` queue claims each job exactly once across two simultaneous instances with terminal rows equal to input (`LAYER_CONCURRENCY` is per-instance, fleet parallelism is instances times that, no fleet-wide ceiling claimed). Deploy artifacts are written and ASCII-verified but NOT built here (no Docker daemon): a multi-stage `Dockerfile` (single process serving the built portal via `PORTAL_DIST_DIR` and the API, HEALTHCHECK on /health), `.dockerignore`, a local-parity `docker-compose.yml` (postgres, a one-shot drizzle push migrate, app, optional seed), `infra/gcp/*.tf` (Cloud Run v2 with a gated `roles/run.invoker` grant to `allUsers` so the service URL is browser-reachable, Cloud SQL PG16, Secret Manager, GCS), and `docs/migration-runbook.md`. Typecheck and build green; full suite green at 888 tests (api-server 493, portal 234, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +35 from AG, all in api-server); long-dash sweep zero on both sides over all 144 public text and jsonb columns across 39 base tables (no schema added). The architect `evaluate_task` returned PASS after one remediation round: the first review caught the GCP Terraform creating the Cloud Run service and outputting its URL but granting no `roles/run.invoker`, so the URL was unreachable by a browser and the runbook `GET /health` smoke test; a gated public-invoker binding to `allUsers` (the `allow_unauthenticated` variable, default true, with a private-edge opt-out) plus the access-model documentation were added and the three workflow gates re-run green before PASS. The honest owner-rerun boundary (docker build and compose up, a full in-container demo seed needing Docker plus live provider keys, a live AWS or GCP run, and terraform apply) is recorded in `phase-AH.md`. Per the owner-authorized sequence Phase AH does NOT pause at its own gate; execution continues to the Phase AI milestone.
- Phase AI gate met (the MILESTONE hard stop closing Stage 5, Platform completion, and the end of the owner-authorized AE-through-AI sequence): verification and the build-report append only, with no product code and no new test, mirroring how Phase M closed Stage 2, Phase V closed Stage 3, and Phase AC closed Stage 4. Each Stage 5 acceptance criterion (AD experience audit, AE ingestion suite, AF local LLM seat and sovereign mode, AG curated custom layers, AH cloud portability) is mapped in `phase-AI.md` to existing tested evidence with the proof type marked honestly: integration against live Postgres (the five ingestion paths and the system-wide raw-absence sweep, the custom-layer create and approve and catalog gating, the multi-instance queue exactly-once claim, the single-process portal serving), deterministic unit test (the AWS SigV4 signer against AWS's vector, the custom-layer template and archetype lockstep, the benchmark cohort math), hermetic conformance with no live model (sovereign mode makes zero external calls, connected makes zero frontier calls from the extraction zone), available-not-connected adapter (the AWS, S3, GCP, and GCS seams until their env is set), or ASCII-verified deploy artifact not built here (no Docker daemon). Typecheck and build green; full suite green at 888 tests, unchanged from AH (api-server 493, portal 234, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4); long-dash sweep zero on both sides over all 144 public text and jsonb columns across 39 base tables, with the Terraform and the root Dockerfile verified ASCII by hand; zero new npm deps. The owner-rerun boundary carried out of Stage 5 (the AF sovereign real-endpoint full seed and the AH Docker build, full in-container seed, live cloud run, and terraform apply) is recorded honestly rather than faked. The architect `evaluate_task` returned PASS. The drift index, the rollup, and the build report are updated to "A through AI". Phase AI closes Stage 5 and PAUSES at the milestone for owner review; it does not auto-advance.
- Post-AI functional and end-to-end verification (2026-06-16): an owner-requested complete drift report at the Phase AI milestone, driving the REAL application in a browser via the Playwright testing skill across four flows. Unauthenticated: the sign-in gate, the honest invalid-login state, and the public `/d/:token` share rendering an honest unavailable state outside the AuthProvider. Authenticated provider-owner smoke: real login through the form, the boot splash, the brief and layers, and the owner-only Admin, Security, and Spend consoles all load. Provider-member negative: owner-only `/admin`, `/security`, `/spend` return the honest NotFound while the provider-allowed `/connections` renders, proving the block is owner-precise. Client-admin negative: owner-only AND provider-only routes return NotFound, `/onboarding` renders, and tenant fencing holds with `/api/tenants` listing only the bound tenant, a 403 on an unbound tenant's overview, and 200 on the bound one. Because owner secrets and SESSION_SECRET reach only the workflow processes, each flow seeded a dev `users` row with a self-generated scrypt hash, logged in through the real form, and was deleted after, returning the shared dev database to baseline. typecheck, build, and test exit 0 with the full suite unchanged at 888 (api-server 493, portal 234, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4); the two-sided long-dash sweep is zero (the source guard in the suite plus a fresh database-wide sweep over 144 text and jsonb columns across 37 base tables, of 39 total). No defect was found and no product code changed (only the drift records and an agent memory note); no phase was minted and no gate advanced; Phase AI remains the milestone hard stop. See `docs/drift/audit-post-AI.md`.
- Phase AJ gate met (MILESTONE hard stop): the Brier-scored calibration ledger, superseding Phase W's loose hits-over-resolved calibration with a proper probabilistic scoring rule. One new `forecasts` table (the only schema change; 39 to 40 base tables) holds one row per binary, resolvable forecast across five kinds (`action_outcome`, `risk_occurrence`, `anomaly_materiality`, `finding_survival`, `confounder_verdict`), with the honesty boundary in the column nullability: `probability` (numeric(5,4)) is set from the REAL Evaluator output at creation, while `outcome`, `resolvedAt`, `brierScore`, and `resolutionBasis` (an enum of measured, modelled, owner) stay null until the forecast actually resolves; the FKs (tenant cascade, layer restrict, committed action and outcome measurement and resolving owner all set null) let the graded row outlive the operator who adjudicated it. The pure Brier math is `(p - o)^2` with the probability clamped, a fixed 0.25 baseline, an empty-set null mean (never a fabricated zero), a per-segment aggregation, a ten-band calibration curve with null empty bands, an honest "early, n resolved" sample label below the threshold (`CALIBRATION_MIN_RESOLVED_PER_SEGMENT` default 10), and a downward-only, threshold-gated, never-inflating confidence calibration. The cortex score stage emits `forecasts[]` as genuine Evaluator likelihoods (no synthesis from a verdict string, no reflexive default, only binary-resolvable claims; confounder verdicts carry `subject_seat=Confounder`), the orchestrator persists them only after the real score stage with `resolveBy = madeAt + horizon`, and the resolution service is the single writer: an `action_outcome` forecast links to its committed action by an EXPLICIT id-or-anchor reference (never a title match), resolves automatically only on a TERMINAL outcome measurement (realized to 1, missed to 0; pending or on_track leaves it open) with the basis carried from the measurement, or resolves by an owner adjudication whose Brier score is computed server-side under an unresolved-row guard that prevents a double-resolve (409). The `/api/calibration` route (behind `requireAuth`) is tenant-scoped for any seat that can reach the tenant and owner-only system-wide, returning the headline Brier against the baseline, the curve, the per-layer, per-kind, and per-seat breakdowns with labels, the resolved and open counts, and the resolved ledger with misses always included; the display-only layer-confidence advisory never overwrites the raw pill. The portal `CalibrationPage` renders the headline with a plain-English explainer, a no-dependency calibration curve, the breakdown tables, honest labels, and the visible ledger, with distinct loading, empty, error, and ready states and a dash (never a zero) for a missing figure. Typecheck and build green; full suite green at 923 tests (api-server 522, portal 240, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +35 from AI: api-server `brierMath` 20 and `calibration.integration` 9, portal `calibrationApi` 6); long-dash sweep zero on both sides over all 150 public text and jsonb columns across the 40 base tables; zero new npm deps. Accepted LOWs (source-reviewed, logged): the score prompt that elicits the likelihoods runs only inside a real paid Evaluator call the suite does not run (the output schema and the persistence ARE test-proven through the real output shape), and the `CalibrationPage` React rendering (its client is unit-tested and its route integration-tested). The architect `evaluate_task` returned PASS. The drift report is `phase-AJ.md`; the drift index, the rollup, and the build report are updated to "A through AJ". Phase AJ is a MILESTONE hard stop: it PAUSES for owner review and does NOT auto-advance to Phase AK.
- Phase AK gate met (not a milestone, but gated; opens Stage 6, the final stage, under owner authorization that cleared the AJ milestone pause to run the AK-AL-AM-AN sequence linearly): the Data Efficacy Index, a per-layer per-tenant 0-to-100 score of how good the DATA feeding a layer is (distinct from the confidence band, which is how sure the reasoning is), computed entirely at READ time from already-persisted state with NO schema change (base-table count stays 40), mirroring the Phase O `connectionHealth` derive-on-read pattern so the index can never drift from the data it describes. The score is a weighted average of five named drivers in one documented, env-overridable config (`lib/efficacy/config.ts`): coverage 0.25 (declared `layers.feeds` present as a connected source, denominator counting only feeds mappable to a connector family through `feedAliasMap`), freshness 0.15 (newest derived signal decayed against a half-life `EFFICACY_FRESHNESS_THRESHOLD_SECONDS` default 86400 and floored by a max-age multiple default 4), verificationRate 0.25 (verified over verified-plus-modelled claims), adversarialSurvival 0.15 (confounders `ruled_out` over total), and sourceDiversity 0.20 (distinct sources against a target default 5), summing to 1.0 and renormalized, each weight overridable by `EFFICACY_WEIGHT_*`. The pure `efficacyMath.ts` composes the index: a null driver is `not_measured`, shown as a dash and accruing disclosed `unknownWeight` rather than counted as a zero, and the outside-in ceiling is enforced IN THE MATH (the connector-grounded coverage and freshness drivers are mode-capped to zero contribution in `outside_in`, so the score can never exceed `modeCeiling = round((1 - coverageWeight - freshnessWeight) * 100)` and a stray connected signal can never lift an outside-in layer past it, while connected reaches 100); `cheapestImprovement` names the single highest-lift next lever and `rollupEfficacy` means the per-layer scores or returns null for an empty set. The read-time service (`efficacyService.ts`) stores nothing: a pure `buildLayerEfficacy` wires the database reads into the math and `loadLayerEfficacy`, `loadTenantEfficacy` (the mean rollup), and `loadEfficacyForTenants` (the portfolio batch) run the queries. The layer-detail route returns `efficacyIndex` beside the confidence band, `GET /api/tenants/:id/efficacy` (behind `requireTenantAccess`) returns the tenant rollup, the portfolio board rows carry `efficacyScore`/`efficacyLayers`, and `portfolioMath` adds an `efficacyRank` (best-fuelled leads, null sorts last) as a second ordering on the value-ranked board. The portal adds the efficacy types, a framework-free `efficacyApi.ts` (401 to unauthorized, a payload without a rollup and a layers array treated as malformed), and the surfaces (`TenantEfficacyRollup` and per-layer `EfficacyNote` on `LayerPage` with the cheapest-improvement hint and the outside-in ceiling, the Board Pack tenant summary, the Portfolio column), each with distinct loading, ready, and error states and a dash, never a fabricated zero, for a not-measured figure. Typecheck and build green; full suite green at 956 tests (api-server 549, portal 246, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +33 from AJ: api-server `efficacyMath` 19 and `efficacyService` 7 plus one `portfolioMath` case, portal `efficacyApi` 6); long-dash sweep zero on both sides over all 150 public text and jsonb columns across the 40 base tables (no schema added); zero new npm deps. The architect `evaluate_task` returned PASS after one remediation round (the outside-in ceiling enforced in both the math and the service, the portal honest states and dash-not-zero, and the weight env-override tests). The two accepted LOWs (the source-reviewed read-time loaders and efficacy routes, and the source-reviewed portal efficacy surfaces) are added to `rollup.md`. Phase AK is not a milestone; the build advances to Phase AL.
- Phase AL gate met (not a milestone, but gated; the second phase of Stage 6, run under the owner authorization that cleared the AJ milestone pause to execute the AK-AL-AM-AN sequence linearly): the decision ledger and the on-demand pre-mortem. Where Phase AK measured how good the DATA feeding a layer is, Phase AL records what a board DECIDES against the intelligence and grades those decisions over time. Three new tables (`decision_records`, `pre_mortems`, `pre_mortem_indicators`; base-table count 40 to 43). The decision ledger holds one hash-chained row per commit, defer, or reject snapshotting the system recommendation and its confidence and basis at that moment (`recommendationHash` binds the row to the exact recommendation), the provenance refs grounding the layer at decision time (`evidenceRefs`, references into the append-only ledger, never raw evidence, an empty array the honest state for an ungraded outside-in layer), whether that snapshot was read server-side or came from the client (`recommendationVerified`), the rationale (hashed into the provenance digest, never raw), the linked AJ forecast, and `contradictsRecommendation` (computed once at decision time). A decision is a recorded human act, so it ALWAYS appends exactly one provenance entry. The two architect-flagged gaps are closed: the commit route reads the recommendation server-side by `actionRef` BEFORE the write transaction (`loadRecommendationSnapshot`, `404 layer_not_found`, `404 action_not_found`, `422 not_an_action` before any action is written) and snapshots the verified SERVER recommendation so the audit can never present a client-typed action as a system one (a no-ref commit keeps the client snapshot, honestly unverified), and `snapshotLayerEvidence` captures the latest ledger entry per claimPath under the layer excluding the decision, challenge, and pre-mortem meta prefixes. The on-demand pre-mortem (`runDecisionPreMortem`) is a REAL Confounder cortex call (`runPreMortem`, real billed telemetry, no stub) with an honest completed-or-failed lifecycle: a completed run writes the ranked failure modes, the residual-risk note, one provenance entry, and one watched indicator per failure mode in `pre_mortem_indicators` in a single transaction; a failed run writes an honest failed row with the error and NO provenance and NO indicators. The indicators feed the Phase Z `premortem_indicator` push rule (`premortemIndicatorDedupeKey(indicatorId, status)` so a status change notifies once and an unchanged active indicator never re-notifies). The board-grade audit timeline (`getDecisionTimeline`) joins the ledger to its pre-mortems and indicators, the committed action, the latest graded outcome, and the linked forecast, computing every figure from persisted state: the running realised value is the cumulative sum of only terminal graded measurements in chronological order (a pending decision carries the prior cumulative forward, never a projection) and "overruled and right" is derived at read time (a contradicting decision is `right` when its `action_outcome` forecast resolves FALSE, `wrong` when TRUE, honestly `pending` until then), never a stored flag. Routes behind `requireTenantAccess` (a client-viewer forbidden from recording a decision or spending a Confounder call): `POST /tenants/:id/decisions` (defer or reject), `POST /tenants/:id/decisions/:decisionId/pre-mortem`, `GET /tenants/:id/decisions/timeline`, `POST /tenants/:id/pre-mortem-indicators/:indicatorId/status`, plus the upgraded commit route. The portal adds the types, a framework-free `decisionApi.ts` (401 to unauthorized), and the `DecisionsPage` and `DecisionControl` surfaces, each with distinct loading, ready, empty, and error states and a dash, never a fabricated zero. Typecheck and build green; full suite green at 1005 tests (api-server 581 across 65 files, portal 263 across 21 files, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +49 from AK: api-server `decisionRecord` 5, `timeline` 10, and `decisions.integration` 17, portal `decisionApi` 17); long-dash sweep zero on both sides over all 169 public text and jsonb columns across the 43 base tables; zero new npm deps. The architect `evaluate_task` returned PASS after one remediation round (the evidence snapshot bound into the provenance digest, and the commit route's server-side recommendation read with its 404 and 422 guards before any write). Accepted LOWs (source-reviewed, logged in `rollup.md`): the on-demand pre-mortem's real Confounder call runs only inside a real paid model call the suite does not run (the route guards, the failed-run row, the indicator wiring, and the timeline rendering of a seeded completed pre-mortem ARE tested), and the portal decision surfaces are source-reviewed (the `decisionApi` client is unit-tested). The drift report is `phase-AL.md`; the build report append is the Phase AL section of `docs/build-report-v2.md`. Phase AL is not a milestone; the build advances to Phase AM (the as-of replay and the diligence pack).
- Phase AM gate met (not a milestone, but gated; the third phase of Stage 6, run under the owner authorization that cleared the AJ milestone pause to execute the AK-AL-AM-AN sequence linearly): the as-of replay read-model and the diligence pack export. One new table (`tenant_layer_snapshots`; base-table count 43 to 44), an APPEND-ONLY per-build snapshot written ATOMICALLY with the in-place `tenant_layers` upsert (same transaction, same instant, the SAME dash-stripped row): it mirrors the live content fields, captures `rawConfidence` for the confidence advisory, a `contentHash` (sha256 over a canonical stable-key serialisation of the content payload, the fingerprint the diff compares), the build-time `dataMode` and `feeds`, and `signalMeta` (the connected-signal metadata that grounded the build, per source its connector key and `computedAt`, the same de-identified pair already in `derived_signals`, never raw content); the efficacy index is deliberately NOT stored but recomputed at read time so it can never drift. `buildTenantAsOf` reconstructs a tenant's state as of a past instant from append-only timestamped state ONLY: per layer the latest snapshot at or before the date (a layer with no build by then is honestly `no_snapshot_available`, never a fabricated empty diagnosis), the efficacy recomputed from that snapshot's captured mode, feeds, claims, confounders, and signal metadata (so a refresh that delete-replaces the live `derived_signals` cannot rewrite a past connected build's coverage or freshness), and the confidence advisory recomputed from the forecasts resolved by the as-of date; the diff is snapshot to snapshot (`diffLayerSummaries`, pure), content-changed by hash and every delta current-minus-as-of and null (never zero) unless both sides carry the figure, plus honest tenant growth (ledger depth as of the date versus now, decisions and outcomes since). The diligence pack (`buildDiligencePack`, `renderDiligencePackHtml`) is a single self-contained brand-styled HTML document assembled from the same persisted state the live surfaces read, carrying the honesty boundary through to the page (verified beside modelled, the efficacy mode ceiling, the calibration sample label, the overruled verdict off the exact `deriveOverruledStatus` contract, the outcome track record, and a provenance integrity banner that flags a broken chain rather than asserting integrity, with every tenant-controlled string HTML-escaped); the render is a PURE function built by hand. Both surfaces are read-only over append-only state and state plainly that history cannot be edited through them. Routes behind `requireTenantAccess` (both GET): `GET /tenants/:id/as-of?at=<ISO>` (400 bad date, 404 unknown tenant) and `GET /tenants/:id/diligence-pack.html` (inline self-contained HTML). The portal adds the as-of types, a framework-free `replayApi.ts`, and the `AsOfReplayPage`, `DiligencePackPage`, and updated `BoardPackPage` surfaces, each with distinct loading, ready, empty, and error states and a dash, never a fabricated zero. Typecheck and build green; full suite green at 1034 tests (api-server 610 across 69 files, portal 263 across 21 files, cortex 110, connectors 29, edge-agent 10, db 8, scripts 4; +29 from AL: api-server `asOfMath` 9, `contentHash` 7, `asOf.integration` 7, and `pack` 6); long-dash sweep zero on both sides over all 183 public text and jsonb columns across the 44 base tables (one table added); zero new npm deps. The architect `evaluate_task` returned PASS after two remediation rounds, the final closing a connected-signal supersession blocker: the as-of efficacy originally read the live `derived_signals` (delete-replaced on refresh) so a refresh after the as-of date erased a past connected build's grounding; the fix captures the build-time signal metadata on the snapshot (`signalMeta`, de-identified references only) and recomputes from it, with a regression proving a post-as-of refresh cannot erase a past build's coverage or freshness. Accepted LOWs (source-reviewed, logged in `phase-AM.md` and `rollup.md`): the diligence pack data assembly and the two read routes have no dedicated integration test (the as-of read-model is integration-tested, the pack render is unit-tested, the services behind the assembly are tested), and the portal as-of and diligence surfaces including the `replayApi` client. The drift report is `phase-AM.md`; the build report append is the Phase AM section of `docs/build-report-v2.md`. Phase AM is not a milestone; the build advances to Phase AN (the final verification and consolidated report that closes Stage 6 and the whole build).
- Phase AN gate met (MILESTONE; the closing phase of Stage 6 and of the whole Elevated Intelligence V2 build, run under the owner authorization that cleared the AJ milestone pause to execute the AK-AL-AM-AN sequence linearly): the final verification and the consolidated report. Phase AN builds NO product feature and changes no product code; its deliverables are a fresh full re-verification and a consolidated build-report append. The configured workflows were re-run fresh in the protocol order (typecheck and build never concurrent with tests): typecheck clean (exit 0), build clean (portal 1765 modules, api-server bundled), full suite green at 1034 tests with zero failures (api-server 610 across 69 files, portal 263 across 21, cortex 110 across 13, connectors 29 across 5, edge-agent 10 across 3, db 8, scripts 4); the only stderr lines are warn-level logs from tests that deliberately exercise failure paths. The long-dash sweep is zero on both sides (the source guard over all authored source including the AN Markdown, and a fresh database-wide cast over all 183 public text and jsonb columns across the 44 base tables); zero new npm dependencies across the whole build. Each named final-stage invariant is pinned by a test that turns red when broken: the Brier maths and downward-only confidence calibration (`brierMath` 20), efficacy movement with honest outside-in-versus-connected ceilings (`efficacyMath` 19, `efficacyService` 7), decision and audit tamper-proofness (`decisionRecord` 5, `timeline` 10, plus the provenance `verifyChain` tests), as-of replay versus snapshot (`asOfMath` 9, `contentHash` 7, `asOf.integration` 7 against live Postgres), and the diligence pack export (`pack` 6). The consolidated report records the scoring design (the data-efficacy index and the Brier calibration ledger, kept distinct from confidence), the efficacy index weights (coverage 0.25, verification 0.25, source diversity 0.20, freshness 0.15, adversarial survival 0.15; env-overridable, renormalised), the decision and forecast schemas (the hash-chained decision record with its server-verified recommendation snapshot, and the forecast whose nullability keeps an unresolved row from carrying a fabricated score), and the honest-labelling rules (modelled versus verified, a dash for a missing figure, available-not-connected, crypto-shred and break-glass honesty, no fabricated telemetry). The architect `evaluate_task` returned PASS with no remediation rounds and no blockers, confirming each invariant is genuinely pinned, that there is no blocker to closing the whole build, and that the report scope is complete. The drift report is `phase-AN.md`; the build report append is the Phase AN section of `docs/build-report-v2.md`. Phase AN is the closing milestone of Stage 6 and of the whole build: the build is COMPLETE (Stages 1 through 6, Phases A through AN, all gated and verified) and there is no next phase.
- Post-AN remediation (2026-06-17): an owner-requested pass after the closing milestone resolved every actionable "Still live, worth attention" item in `rollup.md` (a shared config-gated rate-limit store keyed by a one-way HMAC, ledger append-only enforceable at the database role, cortex write-path injected-model tests, public, efficacy, and as-of and diligence read-route integration tests, the portal pure-logic extraction and its unit tests, and a live 375px measured pass that caught and fixed a real overflow), leaving only genuinely deferred items (chiefly true DOM-rendering tests held off under the zero-new-dependency rule, plus the authenticated sellability share-token route integration test). It minted no phase and advanced no gate; Phase AN remains the closing milestone. Gates re-run green (typecheck and build exit 0; the full suite green at 1130 tests across 131 files, +93 tests in 18 new files); the two-sided long-dash sweep is zero (the source guard over 515 authored files and a fresh database-wide cast over 185 public text and jsonb columns across the 44 base tables). The resolved items moved to "One-time or resolved" in `rollup.md`; the full record is `docs/drift/audit-post-AN.md`.
- Phase AO gate met (not a milestone, but gated; the FIRST phase of the Robustness and Magic wave, the post-AN follow-on wave AO through AS that reopens the build, closed at the Phase AN milestone, to harden the platform and sharpen its surface): six of the connector catalogue's previously declared-only entries become real zero-SDK HTTP runtimes against the uniform connector contract, each running in the in-client edge agent and reducing a provider's public REST or JSON API to ONLY its four declared catalogue signals. salesforce (crm-sales) derives pipeline_coverage_ratio, win_rate_pct, sales_cycle_days, and stage_distribution from SOQL aggregate queries plus one bounded date-only cycle projection; hubspot (crm-sales) the same four from a bounded paged walk of deal properties; quickbooks-online (accounting-erp) gross_margin_pct, revenue_trend_delta, ar_days_outstanding, and expense_ratio from the QBO Reports API Profit-and-Loss and Aged Receivables totals; google-analytics-4 (marketing-web-analytics) conversion_rate_pct, cac_trend_delta, channel_mix_distribution, and engagement_index from the GA4 Data API runReport totals; shopify (commerce-pos-inventory) sell_through_rate_pct, inventory_turns, aov_trend_delta, and stockout_ratio from the REST Admin order and product feeds with Link-header pagination; zendesk (support-customer) csat_index, first_response_hours, ticket_volume_trend_delta, and resolution_rate_pct from the search/count endpoint plus a bounded first-response sample. A shared `httpJson.ts` substrate is the one place a connector touches a provider over HTTP: the Node global fetch with a bounded timeout, a typed `ConnectorThrottleError` on a 429 carrying the Retry-After hint (the runtime owns the retry, never doubled here), `nextLink` for RFC 5988 cursor pagination, and a strict rule that a response body is never logged or attached to an error. The six are added to `IMPLEMENTED_CONNECTORS` and flipped to `implemented: true` (eight implemented in all, with the two warehouse connectors); every other catalogue entry and the warehouse pair are untouched and still report an honest available-not-connected error. Each connector imports only `@workspace/db/contracts`, resolves its credential via `ctx.resolveSecret(scope.authRef)` as a bearer, and returns through `assertDerivedSignalSet`, so no opportunity id, contact email, customer name, channel label, product title, or realm id ever reaches a signal. The honesty boundary is carried through the reduction: a per-connector signal allowlist rejects any draft whose key the connector did not declare, and a figure is OMITTED (a dash, never a zero or an understated partial sum) whenever its population is incompletely observed, including the partial-observability class where a paged walk is truncated at its record cap (HubSpot omits all four when its deal search truncates mid-walk; Shopify folds order-feed truncation into revenue and units completeness and product-feed truncation into on-hand completeness) and the QuickBooks nested-malformed case (aged-receivables completeness accumulates over the whole report tree, so a wholly malformed nested section beside a finite sibling still propagates incompleteness). Tests: `priorityConnectors.test.ts` (34) drives every connector over a node:http loopback harness mirroring each provider's response shape (per-connector derivation, the no-raw-field boundary, honest omission with no zero and no partial sum and no partial-sample figure on a truncated walk, the 429 throttle surfaced as a ConnectorThrottleError with the Retry-After hint, and the credential-by-authRef bearer), plus a signal-allowlist guard pair and a registry integration pair proving all six are marked, registered, and resolved while the warehouse pair and the rest of the catalogue stay untouched. Typecheck and build green; full suite green at 1167 tests (api-server 644, portal 327, cortex 111, connectors 63, edge-agent 10, db 8, scripts 4; the connectors package moves from 29 to 63 with the new 34-test suite); long-dash sweep zero on both sides (the source guard over all authored source including the AO Markdown, and a fresh database-wide row-cast over all 46 public tables reading zero, AO writing no schema and no data); zero new npm dependencies (the Node global fetch via `httpJson.ts`, no SDK). Accepted boundary (logged drift): the six runtimes are proven against a node:http response-shape harness, not the live third-party API, which needs real OAuth credentials and is exercised only on a real tenant connection, mirroring how the warehouse connectors were proven against a real Postgres-wire warehouse while the third-party wires cannot be reached from the build environment. The architect `evaluate_task` returned PASS after two honesty remediation rounds (the QuickBooks nested-malformed aged-receivables propagation, and the HubSpot and Shopify truncation partial-observability omission). The drift report is `phase-AO.md`; the build report append is the Phase AO section of `docs/build-report-v2.md`. Phase AO is not a milestone; the Robustness and Magic wave advances to Phase AP (the sovereign seat realisation).
- Phase AP gate met (not a milestone, but gated; the SECOND phase of the Robustness and Magic wave AO through AS): the sovereign seat realisation, redefined on inspection as a CORRECTNESS AUDIT because the in-boundary sovereign seat was already real and proven from Phases J and AF (the OpenAI-compatible `/v1/chat/completions` adapter in `lib/cortex/src/clients/local.ts` over the Node global fetch, available-not-connected via `resolveLocalSeat`/`getExtractionRuntime`, the two Lens stages routed in-boundary while the external seats receive only profile, Lens output, and math-only grounding, fail-loud with no silent external fallback). The audit found and fixed ONE honesty defect at the as-of replay snapshot sink: the build-time regime was recorded as `dataMode === "outside_in" ? "outside_in" : "connected"`, collapsing a `sovereign` model-execution mode into a connected DATA-SOURCE regime, which would lift a past build's as-of efficacy ceiling to 100 over data it never consumed. The first fix re-read the live `tenants.dataMode` at snapshot time and was rejected by the architect as a mid-build flip race; the race-immune fix threads an explicit `dataSourceMode` (`outside_in` | `connected`) decided once at the seed decision point through `runLayers` and `runLayer`, records the regime the build actually grounded on, and deletes the snapshot read of the mutable column, so a post-build mode flip is preserved as legitimate live/as-of divergence rather than a retroactive restamp. A new third integration case proves race-immunity (the tenant row reads connected while the build threaded outside_in; the snapshot records outside_in with an as-of efficacy ceiling below 100). Typecheck and build clean; the full suite green with zero failures (api-server 647 across 78 files, edge-agent 10, plus the portal, cortex, connectors, db, and scripts suites), the contention-sensitive api-server integration suite passing cleanly on a re-run after a saturated run intermittently flaked one unrelated test; the two-sided long-dash sweep zero (the source guard over authored source including this Markdown and a fresh database-wide cast over all 46 public tables; AP writes no schema and no data); zero new npm dependencies. The architect `evaluate_task` returned PASS after the threaded fix closed the race by construction. AP documents the sovereign seat and the as-of data-source regime invariant in `replit.md`. The drift report is `phase-AP.md`; the index, the rollup, and the V2 build report advance to "A through AP". Phase AP is gated but not a milestone; the wave continues with Phase AQ (the outcome loop closure).
- Phase AR gate met (not a milestone, but gated; the FOURTH phase of the Robustness and Magic wave AO through AS): operational hardening, a posture-and-documentation phase that changes no product behaviour. Two of its three substrate pieces were already real from the post-AN remediation: the config-gated rate-limit store (`RATE_LIMIT_STORE` default `memory`, opt-in `postgres` routing both the auth fixed window and the connector token bucket through shared `rate_limit_*` tables) and the fail-loud provenance append-only database-role SQL (`infra/sql/provenance-ledger-append-only.sql`, granting the runtime role only SELECT and INSERT, aborting under `ON_ERROR_STOP`). AR keeps the code default `memory` so the checks run with no environment, does not rewrite the role SQL, and instead states the production posture, logs it at boot, and aligns the Terraform with the runbook. A new pure module `artifacts/api-server/src/lib/ops/startupPosture.ts` computes and logs two boot posture lines once (a WARN on the in-memory single-instance rate-limit default and an INFO on the shared `postgres` store; and the seven in-process scheduled loops listed with the single-loop-runner requirement), called from `index.ts` after `app.listen`; it is a configuration statement, not telemetry. `infra/gcp/main.tf` pins one always-on instance (`min_instance_count = 1`, `max_instance_count = 1`, was 0 and 4) and sets `RATE_LIMIT_STORE=postgres` with explanatory comments, so the Terraform and the runbook agree. A new `docs/go-live-checklist.md` turns the deploy facts into nine operator checkbox sections (secrets before first boot, SESSION_SECRET stability, owner bootstrap, schema and canonical seed, the REQUIRED append-only role, rate-limit and single-instance scaling posture, backups and disaster recovery, smoke test, rollback); `docs/deploy-readiness.md` gains a single-instance loop-runner section with an honest steady-state-versus-rollout caveat; `docs/migration-runbook.md` marks the append-only step REQUIRED and adds the env bullet, a Scaling posture paragraph, and the env-table row. Tests: a new pure `startupPosture.test.ts` (five tests, no database) proves the posture lines for each store, the loop listing, and the boot emission through an injected logger spy. Typecheck and build green; full suite green at api-server 656 across 80 files (up from 651/79 with the new five-test suite), edge-agent 10, plus portal, cortex, connectors, db, scripts; long-dash sweep zero on both sides (the source guard over all authored source including the AR Markdown and the infra and Dockerfile roots swept manually because they sit outside the guard's configured roots, and a fresh database-wide row-cast over 185 text and jsonb columns across all 44 public base tables reading zero, AR writing no schema and no data); zero new npm dependencies. Accepted boundary (logged drift): the single loop-runner is a deployment posture (one always-on instance), not code-level leader election; a multi-instance request tier needs a separate single loop-runner instance or per-loop leader election. The architect `evaluate_task` returned PASS after one documentation-precision remediation (the steady-state-versus-rollout caveat so "exactly one" loop runner is not read as an absolute during a revision rollout). The drift report is `phase-AR.md`; the build report append is the Phase AR section of `docs/build-report-v2.md`; the index, the rollup, and the V2 build report advance to "A through AR". Phase AR is not a milestone; the Robustness and Magic wave advances to Phase AS (the signature surface).
- Phase AS gate met (not a milestone, but gated; the FIFTH and CLOSING phase of the Robustness and Magic wave AO through AS): the signature surface. AS owns `artifacts/portal` in full and changes no server, route, contract, or shared type (the authoritative diff since the Phase AR commit is portal-only: 67 portal files plus the drift records, nothing outside `artifacts/portal`). It replaces every inline style object with the frozen token vocabulary (`index.css`) and Tailwind utilities, a net reduction of about 376 lines over 67 files; cuts the top navigation to five primary destinations (Brief, Board pack, Layers, Decisions, Outcome loop) with everything else grouped under a role-fenced More menu (no route, guard, or server fence changed, so a surface a seat could not reach stays unreachable); and builds the signature primitives in `components/primitives/`: a new `Signature.tsx` with `SerifDiagnosis` (the single confident conclusion in the serif voice, its tone colouring only the thin leading rule so the conclusion always reads in navy authority and a bad diagnosis is no less confident than a good one) and `GoldUnderlineSweep` (a pure-CSS gold rule that wipes in once per recompute via a remount on a changed `sweepKey`, held fully drawn under reduced motion, decorative and aria-hidden), plus `ProvenancePill` (verified versus modelled), the reasoning ribbon, the headline metric, and the four honest data states. The two signature surfaces, the Morning Brief and the public `/d/:token` diagnosis, are restyled onto these primitives, as is Phase AQ's outcome loop page. The UI copy is normalised to British spelling (organisation, recognised) while the data-contract identifiers stay American by deliberate decision (the `OutcomeMeasurementStatus` `realized` enum, the `realizedValueUsd` fields, and the `catalog` layer keys are wire and code names, not copy; no test asserts the changed strings, verified by grep), closing the normalisation Phase AQ deferred. Typecheck and build clean (build at 1773 modules); the full suite green at 1179 tests (api-server 656 across 80 files, portal 327, cortex 111, connectors 63, edge-agent 10, db 8, scripts 4 including the source guard), the contention-sensitive api-server integration suite passing cleanly green after two saturated runs each intermittently flaked a different unrelated integration test (the `spend` ledger-SUM reconciliation, then the `asOf` deterministic-replay equality), both concurrent-write races on the shared dev database proven to pass serially with file parallelism disabled and structurally unreachable from a portal-only change; the two-sided long-dash sweep zero (the source guard over all authored source including the AS Markdown and a fresh database-wide row-cast over the public text and jsonb columns, AS writing no schema and no data); zero new npm dependencies (the gold-underline sweep and every transition are pure CSS, framer-motion absent). Accepted boundary (logged drift): an automated cross-viewport AA and 375px sweep of the AUTHENTICATED portal is an operator follow-up because the owner secrets are not in the agent shell (no interactive sign-in) and the screenshot tool exposes no viewport control, with AA met by the audited `-ink` text tokens and the 375px floor by fluid wrapping-and-reflowing layouts, both confirmed on the public login surface; a few inline styles are kept where the value is genuine runtime geometry or a runtime-resolved palette colour that cannot be a static token. The architect `evaluate_task` returned PASS. The drift report is `phase-AS.md`; the build report append is the Phase AS section of `docs/build-report-v2.md`; the index, the rollup, and the V2 build report advance to "A through AS". Phase AS is not a milestone; it CLOSES the Robustness and Magic wave (AO through AS).
- Cross-phase drift rollup (A through AS) lives in `rollup.md`; deploy-time operational caveats (in-memory limiter, connector token buckets, SESSION_SECRET rotation, owner bootstrap secrets) live in `docs/deploy-readiness.md`.
- Milestone review report for Phase T (consolidated A through T per Section 4 of the protocol: phase verdicts, the R-S-T requirements checklist with evidence, the anti-gaming affirmation, the consolidated drift items, the logged decisions, and the verification summary) lives in `milestone-T.md`.
