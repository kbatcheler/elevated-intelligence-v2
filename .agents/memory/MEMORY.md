- [Verifying gates and the dash sweep](verifying-gates.md) - gate workflow logs are lossy; run `pnpm run X >/tmp/x.out 2>&1; echo EXIT $?`, and the long-dash ban needs BOTH a source AND a DB sweep that read zero.
- [V2 drift and build-report protocol](v2-drift-protocol.md) - per-phase gate/report steps; an audit/remediation pass AFTER a milestone is NOT a phase (dedicated doc, no fake Phase Y, no gate advance).
- [Async deliver access fence](async-deliver-access-fence.md) - any record-now act-later seam must re-check tenant access at BOTH mint and deliver; a binding can be revoked in between (push leaked until fixed both paths).
- [Secret-in-URL redaction chokepoint](secret-in-url-redaction.md) - a bearer/secret in a URL path must be collapsed to its route template at ONE shared chokepoint before observability capture, else it leaks to Sentry via req.path even when the DB stores only the hash.
- [Push a new table to the dev DB](schema-push-before-integration.md) - a new Drizzle table is not in the live dev Postgres until `pnpm --filter @workspace/db push`; integration tests 500 on the missing relation while unit tests stay green.
- [Evidence-matrix proof-type honesty](evidence-matrix-proof-types.md) - in a verification phase, mark each criterion test-proven vs source-reviewed vs available-not-connected; citing a pure-helper unit test as proof of an engine/route path overclaims and fails the gate.
