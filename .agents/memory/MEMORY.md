# Memory index

- [Code execution sandbox quirks](code-exec-sandbox.md) — executeSql returns text `output` not rows; architect() overflows to context-only when given too many files; bash needs a timeout.
- [DB long-dash sweep gate](dash-sweep-gate.md) — recurring gate step: per-table em/en dash sweep over every public table, dash class built from escapes.
